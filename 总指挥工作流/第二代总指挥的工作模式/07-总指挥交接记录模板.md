# 总指挥交接记录模板

本模板属于抽象层，所有字段由旧总指挥自动读取或生成，不是人工表单。真正使用时，在具体项目管理目录生成实例记录；持久化与逻辑 ID 按索引规范执行，不把项目实例数据写回本目录。便携实例只保留聚合事实和证据指针，轻量加载与展开条件以 `总指挥轻量交接启动配置.md` 为准。

```markdown
# 总指挥交接记录

生成时间：AI 自动记录
项目名称：AI 从项目入口读取
项目根目录：AI 自动定位
状态索引位置与交接记录 ID：AI 自动生成
当前进度视图位置与最后核验截点：AI 自动读取
便携交接快照 ID：AI 自动生成并登记索引
交接类型：正常交接 / 空白账号灾难恢复
灾难恢复热快照：当前进度视图路径、快照版本、事实截点、回读状态
总指挥世代（兼容旧字段名“控制面世代”）：旧世代 / 新世代；旧任务不可得时标记“不可确认”，不得猜测
旧总指挥任务 ID：AI 从平台读取；不可得时使用逻辑 ID
新总指挥任务 ID：接管时由 AI 从平台读取；不可得时使用逻辑 ID
旧总指挥本地会话文件：路径 / 无法定位；不得读取完整 JSONL 作为交接输入
旧总指挥最近会话健康：字节数 / MiB / 检查时间 / 正常、交接评估或优先交接
自动压缩说明：允许同一任务多轮正常自动压缩；压缩次数和压缩前峰值不作为交接依据

## 当前目标

目标版本：
状态：active / paused / superseded / cancelled / completed
目标内容：
范围：
排除项：
验收标准：
停止条件：

## 事实源

项目规则入口：
当前状态入口：
AI 状态索引：
Git/GitHub 当前基线：
相关 Issue/PR 范围快照：
最后可靠状态快照：
最后可靠全量快照：
增量游标或时间截点：
耐久 Commit 台账位置、最后核账截点与 `KEY_NODE` 数量：
最近相关 `KEY_NODE`：提交标识 / 保留原因 / 原提交到当前提交映射 / 证据 / 失效条件
轻量交接启动配置相对路径、版本与 SHA-256：
来源规则清单（02/04/07/09/10 的相对路径与 SHA-256）：
适用的其他项目/仓库规则版本或哈希：
上一轮未闭环对象：
本轮派生对象及交棒状态：

## 紧凑核验基线

快照 ID / SHA-256 / 事实截点：
规则配置与来源指纹：PASS / WARN / FAIL；异常项：
单写者：登记世代 / 旧任务当前状态 / 冲突：
工作区：路径 / 分支 / Head / staged 数 / tracked 修改数 / untracked 数 / 聚合指纹：
远端：仓库 / 默认引用 / Head / 截点后是否变化：
运行进程：端点 / PID / 状态：
运行身份：目标候选 / 代码基线 / 入口与命令 / 规范启动命令及自检输出 / 实际选择器 / 依赖 / 模型资产配置指纹 / 服务来源 / 可见版本 / 失效条件：
并存实现决议：矩阵位置 / 默认与回退 / 当前实际运行 / 未决项：
必要证据指针：存在 / 缺失项：
允许展开的异常范围：

## 人工核验与高成本证据

当前人工核验轮次、基线和状态：
当前轮次证据清单位置：
当前视觉观察任务/逻辑 ID、权限边界与状态：
当前视觉测试包与视觉结果卡位置：
上一关闭轮次、结案摘要和迁移表：
上一轮访问状态与默认读取：closed/cold；false / 待确认
允许回读上一轮原件的触发条件：
原始材料敏感性与存储稳定性：
清理候选清单、保留例外与状态：
下次清理候选审查触发点：
新任务默认不得加载的旧轮次/附件范围：

## 活跃任务

默认主执行窗口、当前阶段与断点：
例外专项任务创建依据与总 Token 净收益：无 / 逐项说明

| 任务 ID | 责任窗口 | 任务对象 | 状态 | 基线 | 已完成证据 | 阻断 | 恢复条件 |
|---|---|---|---|---|---|---|---|

## 本地成果

未提交修改：
本地分支：
worktree：
主执行写入边界与唯一产品写者：
例外专项 worktree/文件隔离边界：
本地安全存档分支与完整检查点链：
耐久 Commit 台账、`KEY_NODE` 与重写/合并映射：
发布归属矩阵与目标 PR Head：
尚未匹配或暂留本地项：
后台进程：
保留原因：

## 人工与外部门禁

待使用者本人确认：
待本轮协作成员：
待其他团队成员：
待领域专家：
待管理员：
待远端 Review/CI：
待补有价值远端动作：
无需新增远端动作及依据：
临时推进但尚未交接：
Assignee 待复核或待调整：
待达到交棒终点：

## 当前授权包

授权来源：
允许对象与动作：
Assignee 修改授权与禁止项：
禁止项：
有效期与失效条件：
新总指挥继承状态：一律不继承；后续按新任务明确授权重新判断

## 交接核验

- [ ] 没有未核验的远端写操作或不可逆动作
- [ ] 所有活跃任务都有可恢复断点
- [ ] 普通实现仍由总指挥主窗口承担；每个例外专项任务都有净收益依据和隔离边界，没有为了角色分工强制下发
- [ ] 当前状态已保存到稳定截点
- [ ] 已记录旧总指挥本地会话文件路径、大小、检查时间和软门禁，或明确标记无法定位；没有读取完整 JSONL 或把自动压缩次数当成交接依据
- [ ] 未提交修改和 worktree 已明确归属
- [ ] 每个项目相关 Commit 已进入耐久台账；`KEY_NODE` 的标识、保留原因、映射、证据和失效条件完整，文档压缩未删除这些字段
- [ ] 人工核验或恢复所需的目标候选、实际入口、规范启动命令及自检输出、选择器、依赖、模型/资产、服务来源和可见版本可恢复；并存实现及未决选择没有被默认值、一次性启动命令或旧进程掩盖
- [ ] 新旧总指挥不会并行调度；同一冲突域没有总指挥与专项任务并行写入
- [ ] 当前授权包已核对，未把旧授权自动继承给新目标
- [ ] 范围、动作回执、有价值远端动作和交棒四类差集为空或已进入明确门禁；“无需新增远端动作”均有依据
- [ ] 最后可靠全量快照、增量游标、未闭环旧账和派生对象均可恢复
- [ ] 已记录本轮适用规则版本，未把执行中更新的规则静默追溯应用
- [ ] 当前进度视图已完成健康检查；如发生压缩，移出内容仍有可回查指针且当前结论等价
- [ ] 便携交接快照包含事实截点、禁止项、证据入口和失效条件
- [ ] 便携交接快照包含轻量启动配置路径/版本/指纹和 02/04/07/09/10 来源指纹；新候选已建立独立加载记录
- [ ] 首次只读只输出聚合计数、指纹和 PASS/WARN/FAIL；只对异常项做最小展开，没有读取完整旧任务、Git 清单、Diff、日志或冷截图
- [ ] 空白恢复时已把批次分为已完成不可重复、执行中断、尚未开始和待确认，且原队列顺序未被后续消息覆盖
- [ ] 新任务没有继承旧远端授权、运行中进程、专业签认或旧任务“已读规则”状态
- [ ] 灾难恢复热快照已在接管后回读关键字段并登记新世代；旧任务是否仍运行不确定时未虚报为已停止
- [ ] 当前与上一人工核验轮次已分开；中央索引只保存轮次清单指针，没有复制逐张图片索引或正文
- [ ] 已关闭轮次标记为 `closed/cold` 和 `default_read=false`；接管首次只读没有主动枚举或打开旧轮次原图
- [ ] 需要回读旧原件时已命中明确触发条件、记录证据 ID，并限制为解决冲突所需的最少附件
- [ ] 图片密集新轮次的交接只携带摘要、未解决项和清单指针；没有把旧图片或逐图分析复制进新任务
- [ ] 当前视觉观察任务未取得总指挥权、代码写权、中央状态写权或远端授权；只写唯一指定的本轮证据清单/结果卡
- [ ] 总指挥交接只携带视觉结果卡、证据清单指针和最小回读条件，没有复制观察任务聊天或重新上传整批原图
- [ ] 清理候选审查与实际删除分开；交接记录中的清理状态没有被误当成删除授权

## 新总指挥第一次只读动作

操作者可直接使用 `01-操作者操作手册.md` 的“场景 6A：新总指挥接收正常交接”启动本节。模板只保存项目事实和第一次只读动作，不在此复制操作者提示词，以免两处规则漂移。

1. 完整读取 `总指挥轻量交接启动配置.md`，建立本任务加载记录并比对配置及来源规则指纹；异常项按配置展开。
2. 按“快照 → 项目入口 → 当前进度视图当前区 → AI 状态索引当前指针 → 唯一活跃任务当前阶段”的最小链读取。
3. 用聚合计数、Head、指纹和 PASS/WARN/FAIL 核对当前 Git/GitHub 基线、单写者和任务断点；只有异常项才展开。
4. 只刷新截点后变化、未闭环、派生交付物和交接记录中标记为“受影响/待确认”的对象。人工核验优先读取当前轮次清单和上一轮结案摘要；上一关闭轮次原图默认不读取。
5. 若快照或游标不可靠，先扩大受影响部分核账；仍无法恢复时才退回全量。候选阶段不启动任何推进。
6. 候选报告可切换后等待操作者停止或归档旧总指挥并发送最终确认；再做最小失效复核，通过后按索引规范更新当前总指挥、交接记录和可靠截点并回读。授权有效性按新任务重新核验。

旧任务不可读或不可写时，不要求旧总指挥完成主动交接。新总指挥改按“入口 → 灾难恢复热快照 → 状态索引 → 当前 Git/平台事实 → 受影响冷证据”恢复；先核对已完成凭证和执行中断点，禁止从第一步重跑整个队列。恢复可靠后写入新控制面世代；事实或修改归属仍冲突时保持只读并列出最小缺口。

## 例外专项或临时 AI 协作返回包

通过净收益门禁的例外专项或临时 AI 未取得总指挥调度权时不得直接修改中央状态；可以更新便携快照副本。结束时直接向总指挥返回：更新后的快照、实际完成、代码/远端变化及回执、验证、未完成项、阻断、下一责任人、快照是否失效，以及建议写回的最小状态增量；不要求使用者人工转发。视觉观察任务是更窄的临时角色：只返回本轮视觉结果卡和证据定位，不返回整批图片或接管建议，也不把观察到的表象直接升级为代码根因或专业结论。

## 不应从交接记录直接推断的内容

- 当前 GitHub 状态；
- 当前模型或 Codex 窗口真实配置；
- 任务是否仍在运行；
- 领域专家或队友是否已经收到消息；
- 任何远端写操作是否已经完成。
- 本轮使用者本人、本轮协作成员和目标是否仍与上一轮相同；必须以当前指令为准。
- 旧授权是否仍有效；新目标或新轮次不得从交接记录静默继承权限。
```
