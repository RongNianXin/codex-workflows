# 第二代工作流操作手册

本手册是操作者作为人类唯一需要打开阅读的行为入口。`00、02～10` 和非编号的 `总指挥轻量交接启动配置.md` 是供 AI 按场景自动读取的规则、模板与启动配置；下文每个场景中的“读取文档”表示要让 AI 读取，不要求你再打开、理解或手工整理这些文件。

## 下载后 3 步启动

1. 下载或克隆本仓库到一个稳定的本地目录，确认该目录直接包含 `00～10` 文档和 `总指挥轻量交接启动配置.md`。
2. 在 Codex Desktop 中打开真正要开发或管理的目标项目，而不是本规则仓库；只有维护规则本身时才把本仓库作为目标项目。
3. 复制下方“登记规则文档目录”提示词，把占位符替换为本仓库的本地绝对路径；登记通过后发送“场景 1”，再按需要选择其他场景。

如果 Codex 无权读取规则目录，请先把该目录加入当前可读工作区，或只把 `00～10` 复制到目标项目中已被 Git 忽略的本地规则目录，再登记新路径。目标项目没有合适的状态目录时，AI 可能要求你确认一次 `AI状态索引.md` 的存放位置；应选择项目已有的个人、私有或本地管理目录，并确保它不会被提交到团队仓库。

## 使用者须知
1. 本工作流仅直接适用于 GitHub；其他平台要先单独适配。
2. 打开一个总指挥任务，从下表选择场景并复制提示词；拿不准时先用场景 1 恢复事实。目标、反馈和结果默认只告诉总指挥，不要向多个窗口重复描述。
3. 只填写 `【】` 中 AI 无法读取或不能替你决定的内容。多个目标或账号用逗号分隔；示例只帮助理解，不是固定答案。任务 ID、分支、截点和平台事实由 AI 自动处理。
4. 协作任务默认先只读扫描并生成执行卡；你确认后，总指挥才执行卡内修改和远端动作。确认本地执行卡不等于授权 Push、PR、Comment、Review、Approve、Merge 或部署。
5. 默认由总指挥单窗口实现和验证。只有独立并行、独立审查、材料隔离或能力缺口能降低整个工作流成本时，才创建最少量专项任务；你不需要转发专项结果。
6. 场景名称只表达意图。AI 必须根据当前仓库、责任、分支和风险自动纠正场景，并代你判断 Review、实现责任和分支载体；涉及责任接管、远端写入、历史覆盖或不可靠基线时再请求确认。
7. 本地修改、测试和 Commit 都不代表已经发布。AI 不得清理、覆盖或夹带来源不明的修改；作者自检和你的体验确认也不能替代仓库要求的独立 Review、CODEOWNER 或专业验收。
8. 每轮结束，AI 应用人话说明成果、证据、剩余风险和是否需要你操作。需要人工参与时必须给出操作、目的、预期结果和异常反馈方式；精确授权文本可以直接复制发送。
9. 是否换任务由 AI 根据稳定截点、未完成工作和会话健康判断。多次自动压缩不等于异常；`200 MiB` 只触发交接评估，`300 MiB` 才在当前原子步骤安全收口后优先交接，两者都不是 OpenAI 官方限制。
10. 首次使用、换电脑或移动规则目录后执行一次“登记规则文档目录”。之后直接用自然语言描述目标即可；AI 从已登记入口按场景读取规则，不要求重复上传全部文档，也不从旧任务继承授权。
11. 长任务默认低干扰运行：重型构建、完整测试、模型和浏览器任务串行；浏览器回归默认无头单 worker。明显卡顿时直接要求“暂停新的重型步骤并切换低干扰模式”。
12. 图片密集验收只有在隔离收益明确时才使用一个只读视觉观察任务；少量截图留在总指挥。关闭轮次后把原件设为默认不回读并不等于删除，实际删除仍需单独确认。

### 长任务期间保持电脑可用

- 你可以在 AI 执行长任务时继续编辑文档或浏览网页。默认优先保持前台可用，而不是追求最快完成。
- “后台运行”只能避免窗口抢焦点，不能单独降低资源占用；AI 还必须限制浏览器 worker，并让构建、完整测试、模型和 GPU 验证依次执行。
- 必须观察真实页面且执行前已经可以预见时，AI 会在执行卡或启动摘要中告诉你。运行中临时发现需要可见窗口时，告知只是一条非阻断通知，AI 可以直接打开并继续，不把它变成新的确认步骤。普通自动回归仍应使用无头模式。
- 如果你感到明显卡顿，直接说“暂停新的重型步骤并切换低干扰模式”。AI 应先保存断点、回收自己创建的空闲窗口和服务，再以单 worker、最小测试范围继续；不会结束你的个人或系统进程。

快速定位：

| 现在要做什么 | 直接使用 |
| --- | --- |
| 首次使用、换电脑或移动了规则文件夹 | 登记规则文档目录 |
| 首次启动或恢复总指挥 | 场景 1 |
| 新账号完全没有历史，或旧总指挥任务无法继续 | 场景 1A |
| 提出普通开发目标 | 场景 2 |
| 把会议、问题或规划转为 Issue，并继续实现到 PR | 场景 2A |
| 从一个新的独立目标开始，在本地多轮实现和人工验收后再集中发布 | 场景 2B，子场景 A |
| 把已经在远端协作过的目标从当前断点转为本地闭环 | 场景 2B，子场景 B |
| 全量接管当前协作队列中的 Issue/PR | 场景 3A |
| 只检查协作队列的新对象、新回应、代码变化和历史漏项 | 场景 3B |
| 在新任务中精确解释上一轮某个 Issue/PR | 场景 4B |
| 汇总进展、人工操作、目标变更、交接或复盘 | 场景 4～7 |
| 立即暂停 | 场景 8 |

全量推进适合首次接管、范围或协作者变化、长期未处理、怀疑遗漏或历史截点不可靠时，用来重建当前完整协作队列。增量推进适合已有可靠截点后的高频交替协作，只检查新增/变化对象、未闭环旧账、派生对象和最小依赖；它不是只看新编号。任务数量少不等于可以直接增量，新开任务也不等于必须全量，关键看可靠截点能否恢复。拿不准时可以直接使用场景 3B，由 AI 验证截点，不可靠时自动转为全量。

场景 2B 适合某个功能由你承担主要实现责任、希望先在本地连续迭代，等人工表现和自动验证都稳定后再集中发布。拿不准是子场景 A 还是 B 时保持“自动判断”；如果目标同时包含新功能、已有 PR 和队友负责的依赖，AI 应拆成多个工作包，不能强行把整个目标归入一个子场景。

账号、范围门禁、责任判断、远端授权、语言和输出细则由核心规则统一维护；操作手册不重复展开。使用者本人、本轮协作成员或其他 AI 的建议都要先核对事实和风险，不能直接当成成立的结论。

## 首次使用、换电脑或移动目录：登记规则文档目录

只有首次使用、规则文件夹被移动或换电脑时需要执行一次。请复制下面内容，把路径替换成当前存放 `00～10` 文档的文件夹；平时执行场景 2A、2B、3A、3B 等任务时不必重复填写。

```text
第二代规则文档目录：【本地绝对路径】(Windows 示例：C:\path\to\codex-workflows；macOS/Linux 示例：/path/to/codex-workflows)
请验证该目录包含《00-第二代工作流总览.md》，并按其中编号注册表核对规则文件。验证通过后，把新的规则根目录、注册表版本和逐文件指纹写入当前项目的 AI 状态索引；旧路径标记为失效。只登记规则入口，不修改产品代码，不执行远端写操作。
```

预期结果：AI 明确回复“规则目录验证通过并已登记”，列出识别到的规则版本和缺失文件。若路径不存在、目录不完整或状态索引不可写，AI 必须停止登记并说明具体问题，不能假装成功；你只需提供新路径或修复后的文件夹，不必逐个上传文档。

## 场景 1：首次启动或恢复总指挥

读取文档：按 `02` 的标题级加载映射读取不可压缩内核，并读取 `09` 核心原则和 `10` 的规则入口/当前指针章节；影响范围不可靠时才全文回退

复制发送：

```text
第二代规则文档目录：【自动定位 / 本地绝对路径】(Windows 示例：C:\path\to\codex-workflows；macOS/Linux 示例：/path/to/codex-workflows)
首次使用、换电脑或移动过规则文件夹时填写当前绝对路径；其他情况保留“自动定位”。
请按 02 的标题级加载映射读取 02、09、10 的启动内核，在当前项目建立或恢复唯一总指挥；记录实际读取标题，影响范围不可靠时再全文回退。优先从项目状态索引登记的本地规则根目录读取；入口缺失时再请我提供一次规则文件夹路径，不要要求我逐个重复上传规则附件。
先只读确认项目范围、规则入口、当前目标、现有任务、代码/Git 状态和远端状态。
判断这是首次启动、正常恢复、目标变更还是控制面冲突。
不要修改产品代码，不执行远端写操作，不创建重复状态记录。
最后只汇报：当前目标、事实基线、活跃任务、阻断、下一波次和需要我确认的事项。
```

## 场景 1A：空白账号灾难恢复

适用情况：新账号看不到任何旧任务，或虽然能看到旧对话但无法继续发送消息。你只需要在真正的目标项目中新建一个 Codex 任务，并复制下面一段；不需要先找旧任务 ID、历史卡片 ID 或逐条解释项目。

读取文档：先从项目规则定位总指挥入口，再读取当前进度视图、`AI状态索引.md`，并按恢复缺口读取第二代 `02、04、09、10` 对应章节；影响范围不可靠时全文回退。

```text
我现在明确任命你为本项目的新总指挥。请执行一次“空白账号灾难恢复只读接管”。

先从当前项目规则定位个人管理入口；读取总指挥入口、当前进度视图和 AI 状态索引，再按 02 的标题级加载映射和恢复缺口读取第二代 02、04、09、10；影响范围不可靠时全文回退。若规则根目录失效，只向我索要一次新的规则文件夹绝对路径，不要要求我逐个上传文档，也不要全盘盲搜。

把旧账号、旧任务和聊天历史视为可能完全不可用。只读核对：项目范围、当前目标与暂停目标、队列顺序、专项任务断点、工作区/分支/worktree/未提交修改、最近验证、后台进程、Git/GitHub 当前事实、禁止项、外部门禁和详细证据入口。已有完成凭证的批次不得重复执行；快照与当前事实冲突时，以当前代码、Git/平台和可回读证据为准并标记失效范围。

本次只做恢复，不修改产品代码，不执行 Push、PR、Issue、Comment、Review、Approve、Merge、工作流重跑、部署或其他远端写操作；旧授权、旧运行状态和专业结论一律不自动继承。

先输出：恢复置信度、已确认事实、待确认项、暂停/执行队列、精确断点、下一安全动作和是否需要加强核账。事实可靠后，更新当前进度视图、AI 状态索引和新的总指挥世代/接管截点；再等待或按我当前明确指令继续。
```

预期结果：AI 能在不读取旧聊天的情况下说明“项目现在做到哪里、哪些已完成不可重复、哪个队列正在暂停、下一步从哪里继续”。如果热快照缺失或与工作区冲突，AI 应停在只读恢复并列出最小缺口，不得猜测或清理文件。

## 场景 2：提出一个普通开发目标

读取文档：按 `02` 的标题级映射读取普通目标内核和当前风险对应的 `09`。

复制发送：

```text
请围绕下面目标规划并执行一个有限 Goal：
目标：【本轮目标】
补充限制或业务验收：【无 / 具体限制或验收要求】(示例：只修改网页端，不调整后端接口)
请自动读取当前项目、规则、状态索引和相关代码，先草拟范围、验收标准、排除项、基线与停止条件。只有不同解释会显著改变范围、风险、成本或对外行为时，才向我提出最少问题。
第一阶段先读取团队和仓库规则，只读扫描最小必要事实并做必要验证；不要修改产品代码，不要进入主执行或分配具体实现任务，不要执行远端写入。
完成范围、依赖、风险和验证计划后，生成一张人话批量执行卡：推荐执行/不执行、最坏后果、硬禁止项、自动执行范围和紧急停止条件；技术精确值放附录。
收到我一次集中确认后，由总指挥在当前主窗口无人值守执行。只有能证明对整个工作流有净收益或必须独立审查/隔离时，才创建最少量专项任务；不要让我向两个窗口重复描述或转发结果。授权外动作只暂停受影响项，继续其他已授权工作，最终统一汇报。
```

## 场景 2A：把会议或规划转为 Issue，并端到端推进

读取文档：按 `02` 的标题级映射读取 2A/必要 3A/现有 PR 章节，按需读取 `04` 与 `09`。

默认使用端到端推进；只想建档、不改代码时，把“推进模式”改为“仅创建 Issue”。下面的可选设置已经填好默认值，没有特殊安排时直接保留；多个值统一用逗号分隔。复制发送：

```text
请把以下会议结论、问题描述或规划转为可执行工作项，并按本轮推进模式处理。
使用者本人平台账号：【平台账号】
目标模块或母任务：【一个或多个目标】(示例：目标模块1，目标模块2)
本轮协作成员平台账号：【一个或多个平台账号 / 无】(示例：账号1，账号2)
推进模式：端到端推进（只想建档时改为“仅创建 Issue”）
历史协作任务处理：只登记、不执行（希望本轮完成后继续处理时，改为“完成本轮后继续处理”）

Issue 负责人账号：【未指定 / 一个或多个平台账号】(示例：账号1，账号2)
Issue 候选负责人账号：【无 / 一个或多个平台账号】(示例：账号1，账号2)
候选负责人只用于提供人选，不得自动设为 Assignee。
本轮实现与 PR 推进责任：【使用者本人优先 / 指定平台账号 / 按对象分别指定 / 由 AI 根据现有责任证据建议】
责任依据或对象例外：【无 / 具体要求】(示例：小组会议决定：目标模块的 Issue 由使用者本人提出并优先实现；协作成员主要负责交叉审阅)
代码 PR 审阅人账号：【自动按仓库规则判断 / 一个或多个平台账号】(示例：账号1，账号2)
代码 PR 审阅人负责检查后续代码改动，不是 Issue 负责人；仅创建 Issue 时不会请求其审阅。
不同目标的人员安排：【无 / 按目标说明人员安排】(示例：目标模块1由账号1实现、账号2审阅；目标模块2由账号2实现、账号1审阅)
填写“无”表示全部目标共用上面的设置。
最低有效批准数：由 AI 按仓库规则自动读取；只有需要提高门槛时才把本行改为明确数字

本轮新增附件或原始记录：【无 / 本地路径或附件名称】(Windows 示例：C:\path\to\meeting-notes.txt；macOS/Linux 示例：/path/to/meeting-notes.txt)
补充业务限制或非平台交办：【无 / 可核验摘要】(示例：会议决定先完成网页端，再处理移动端)

先读取团队和仓库的 Issue 模板、标签规范、贡献规则、分支保护、Review 和合并门禁；扫描现有 Issue/PR 去重并建立依赖拓扑。附件先按必要、辅助、重复分类，已有可靠摘要且原件未变化时不要重复读取。

若存在未闭环的集中接管队列，按核心规则先做低成本兼容性扫描，防止重复对象、错误 Base 和堆叠冲突；该扫描本身不等于授权完整 3A。

第一阶段只读生成一张合并批量执行卡，覆盖：建议创建或更新的 Issue、正文与标签、责任安排、实现单元、任务分支、Push、真实代码或文档 PR、Issue 关联、Reviewer 请求、验证、失败回退、禁止项和交棒终点。Issue 编号尚未生成时使用稳定草案键；创建后自动回填编号和关联，不要求我再次确认。

我集中确认一次后，在同一母任务中连续执行：创建并回读 Issue → 推进当前可执行项 → 修改与测试 → Push 并创建真实 PR → 满足仓库条件时转 Ready 并请求 Reviewer。不得创建没有真实交付的空 PR；Epic、调研、决策或领域取证 Issue 不强制对应 PR。

缺少 Reviewer、辅助附件或其他非当前阶段条件时，只暂停受影响动作并继续其余工作。不得违反仓库规则；推进到规则允许的最远位置后，统一报告待补门禁和通用流程不适配项。Approve、Merge、Close、部署及其他高风险动作仍按授权规则单独处理。结束时按核心规则自动输出 PR 堆叠树、Issue 依赖树、当前行动方，以及使用者本人的必须/建议人工操作。
```



同一活动母任务讨论期间，后续问题视为修正增量。全部细节确认后，可以只发送：

```text
请执行当前母任务的完整已确认方案。合并后续全部修正，以最后一次明确结论为准；未决事项排除，远端写入先生成一张合并批量执行卡。
```

只有存在唯一活动母任务、已确认结论和排除项可以可靠恢复时，这条短指令才生效；否则总指挥先列出冲突或待决项，不得只执行最后一条细节。


## 场景 2B：单窗口主执行的本地闭环与集中发布

读取文档：按 `02` 的标题级映射读取 2B/现有 PR/工程章节，按需读取 `04`、`05`、`09`、`10`。

这个场景只改变“实现和验收主要在本地连续完成”的节奏，不降低仓库规则，也不自动取得远端写入权限。它分为：

- 子场景 A：没有需要继承的远端协作对象，从新的独立开发目标开始；仍须核对当前默认分支、仓库规则和必要依赖。
- 子场景 B：相关 Issue/PR 已有 Comment、Review、Commit、Push 或责任记录，从可核验的远端断点转为本地推进；旧记录全部保留并作为输入证据。

### 1. 启动并自动判断场景

复制发送：

```text
请启动场景 2B，对下面目标执行“本地闭环实现与集中发布准备”。
目标模块或功能：【具体目标】
希望使用的子场景：【自动判断 / A-新的独立开发目标 / B-远端协作断点转本地】
关联 Issue/PR：【自动发现 / 编号或链接 / 无】
需求与证据材料入口：【自动发现 / 一个或多个本地文件、目录或可访问链接 / 无】(示例：会议总结、规范附件和错误示范所在的资料包目录)
使用者补充目标与人工关注点：【无 / 我额外希望达到或重点观察的表现】(示例：优先观察输出曲线与局部参考方向是否一致，以及缩放后是否仍保持几何关系)
补充范围或禁止项：【无 / 具体限制】(示例：只改网页端算法，不改后端接口和数据格式)

先读取当前团队与仓库规则、状态索引、Git 工作区、当前分支/工作树、默认分支、未提交修改和相关 Issue/PR 元数据。把我选择的子场景只当作提示，自动判断它是否适用，并用一句话回显：实际场景、关键依据、置信度和是否需要改道。

对“需求与证据材料入口”先读取资料包入口或索引，再按相关性分层展开，不默认每轮重读全部逐字稿和附件。把材料分类为：当前正式需求或团队决定、已确认代码/项目事实、项目候选规则、参考资料、反例或回归样例、过时/重复材料、冲突项、待专业确认事项。会议中的实现举例、AI 摘要和未经确认的专业参数不得自动升级为硬规则。

根据材料和当前代码自动生成“需求—证据—验收矩阵”，逐项写明：目标、证据来源与强度、拟实现程度、自动验证、需要人工观察的现象与样例、人工核验插入时机、验收责任方、通过/失败标准、专业或外部门禁。使用者补充内容只作为增量，不要求我把全部附件和标准压缩进一个占位符。能从材料可靠推导的内容由你自动完成；只有证据冲突、产品取舍或责任人无法确定且会显著改变方案时，才向我提出最少问题。

路由至少区分：A-新目标、A-恢复已知本地工作、B-本人负责对象续作、B-他人负责对象推进、混合场景、不适用、来源不明修改的恢复场景。高置信度且不改变责任、远端权限或历史的低风险误选可以自动纠正；涉及接管他人对象、覆盖分支、远端写入、基线不可靠或来源不明修改时，先停下来生成最小确认项。

对每个现有 PR，先判断本轮母目标是“纯复审”还是“交付推进”。再分别给出：是否需要立即提交 Comment、Request Changes、Approve 或不发 Review；应由使用者本人直接修复、交回原作者、由维护者修复还是等待协调；应使用本人现有安全分支、基于锁定 Head 的独立修复分支、经明确允许后更新原 PR 分支，还是暂不建分支；修复后当前处理者是否仍满足有效 Approve 条件。用简短人话解释依据和未选方案。不要把 Review 当作实现前的固定步骤，也不要把写权限当作修改他人分支的授权。

只读预检后生成一张“本地闭环执行卡”，写明：起始基线、隔离分支或 worktree、关联远端对象及继承证据、需求—证据—验收矩阵、材料冲突与缺口、责任边界、允许修改范围、禁止动作、自动验证、分阶段人工验收步骤、专业验收门禁、本地 Commit 检查点、主线同步时机、停止条件和最终发布门禁。执行卡还要给出低干扰运行计划：识别重型步骤，默认串行；浏览器自动化默认无头单 worker；已知需要可见窗口时在卡内预告，运行中临时发现时直接打开继续且不等待回复；未要求持续试用时在验证后关闭浏览器和开发服务。不得承诺持续 CPU/内存百分比。执行卡确认前不修改产品代码。

我集中确认本地闭环执行卡后，由你作为总指挥在当前主窗口连续修改、测试、构建、保存必要截图或日志，并在长任务或阶段边界建立本地 Commit；这些动作都不得 Push。只有通过总 Token 净收益门禁时才创建最少量例外专项任务，不要让我重复转述。每轮修改代码并达到可人工观察状态时，先确认正确分支/worktree，后台启动或重新核验本地服务，检查本轮 localhost 页面可访问并直接在浏览器打开，同时把新的可点击地址发给我；不得默认让我自行寻找地址或沿用失效的旧地址。若受系统限制确实无法打开，说明失败原因和最短异常处理。我的反馈不改变范围时，直接在同一隔离工作区继续修改，不要为每次本地返工重新申请远端授权。

不得清理、覆盖或吸收来源不明的本地修改；不得静默接管队友负责的 Issue、PR 或分支。子场景 B 必须保留旧 Review 和未闭环意见。问题明确、可复现且使用者本人有实现意愿时，AI 必须比较两条载体：团队规则、分支权限、责任安排和并行状态都允许时，可优先直接更新原 PR 分支以减少重复集成；归属或并行修改不清、需要隔离回退、原分支不可写或直接更新会越权时，改用基于锁定 Head 的独立修复分支。需要原作者作设计决定、对方正在修改相同范围、责任未明或专业门禁未解除时，才交回或等待协调。

人工回归尚未完成时可以提前执行只读的发布归属预审，建立“本地提交/代码块 → 现有 Issue → 现有 PR Head”的归属矩阵和不可执行草案；我明确接受当前表现且自动验证满足要求后，执行正式发布候选审查。若时间或业务紧急，我也可以明确列出未完成验收、接受风险并选择紧急发布豁免审查；这不会把未验收改写成通过，也不覆盖仓库或平台硬门禁。两条可执行路径都要同步并复核最新默认分支、完整本地检查点链、允许范围内冲突、受影响验证、验收/豁免证据和远端事实漂移。技术 Commit 可以多对多映射，不得仅按标题、标签或文件名猜测归属。原存档分支不得因发布拆分被重写；多个目标 PR 应分别从锁定 Head 建立隔离 worktree。没有我对正式卡内相应动作的最终确认，不得 Push、创建或更新 PR、Comment、Review、Approve、Merge，也不得修改 Issue、Assignee 或标签。
```

预期结果：AI 先给出场景判断、材料分级、需求—证据—验收矩阵和本地闭环执行卡。你确认一次后，总指挥在同一主窗口连续推进，并在矩阵规定的节点给出最小人工操作包和本轮有效 localhost；真正要写入 GitHub 时，AI 再单独给出远端发布执行卡。

### 2. 反馈一次本地人工验收

看到 AI 给出的人工验收操作包后，按实际情况复制发送；不需要描述代码方案。AI 默认每次只给 1～2 个能够清楚对应反馈和截图的微步骤，并说明操作位置、具体动作、观察重点、正常结果和异常截图范围；你反馈后由 AI 判断是否需要写入记录或归档证据，再继续下一组：

```text
本轮本地人工验收结果：【通过 / 未通过 / 部分通过】
观察到的现象：【具体表现】
复现输入或步骤：【沿用 AI 给出的步骤 / 补充说明】
希望优先修正：【最影响结果的问题】
请保持当前场景、范围和远端禁止项，在同一隔离工作区继续本地迭代；如果反馈会改变公共接口、数据格式、责任边界或原执行卡范围，先说明影响并生成最小补充卡。
```

#### 结束图片密集的大轮次并准备下一轮

完成 A 轮全部阶段后，发送下面这段；你不需要自己整理文件哈希或技术 ID：

```text
请收口当前人工核验大轮次【轮次名称或“自动读取当前轮次”】。先冻结问题和阶段结果，生成结案摘要、问题迁移表和轮次证据清单；每张截图只记录对应阶段、观察结论、路径/指纹、敏感性和存储稳定性，不把图片内容复制进中央状态。

将本轮原始截图转为冷证据并设置默认不回读。不要删除、移动或复制任何原件；只生成清理候选和保留例外。以后只有回归、事实冲突、摘要不足、安全/专业/合规/审计门禁或我明确要求时，才回读解决问题所需的最少原图。

如果下一大轮次仍会上传较多图片，在形成可靠断点后给我一段可复制的新任务接管提示词。新任务默认只加载新轮次清单和当前未解决项，不主动读取已关闭轮次的附件。
```

不要为八个小阶段开八个任务。少量图片、需要与代码修改连续对照或结果卡不足以替代原图时，继续使用当前总指挥任务。只有长寿命主窗口会被大量原图显著拖累且隔离净收益明确时，才采用“长寿命总指挥保持文字主线 + 当前大轮次一个短寿命视觉观察任务”。两种路径都依赖稳定断点；单纯把本地索引标成不读取，不能抹掉当前聊天已经上传的图片。

#### 在图片进入总指挥聊天前使用视觉观察任务

如果预计本轮会连续上传多张截图、原始分辨率图片或视频，不要先把它们发给长寿命总指挥。先让总指挥生成本轮文字测试包，再新建一个 Codex 任务，将下面提示词和测试包发给它；一个大轮次只开一个观察任务：

```text
你是本轮只读“视觉验收观察员”，不是总指挥，也不是代码实现者。

请读取总指挥提供的文字测试包：【粘贴测试包，或填写其稳定本地路径】
本轮 ID：【由总指挥生成】
当前代码/页面基线、URL 与工作区：【由测试包给出】
本轮证据记录或结果卡位置：【由总指挥指定；没有可写位置时只在回复中生成结果卡】

我会按测试包分批上传本轮截图。你只分析当前轮次的图片和文字反馈：
1. 每批先按步骤 ID 记录可直接观察到的事实、通过/失败/待确认、证据 ID 和仍需补充的最小信息；不要猜测代码根因或专业参数。
2. 允许在指定的本轮证据记录中写入或更新观察结果；禁止修改产品代码、总指挥热快照、AI 状态索引、其他任务记录、Git/GitHub 或任何远端状态。
3. 不要求我把同一图片再次发给总指挥；不要把原图、图片二进制、逐像素长描述或整段聊天复制进结果卡。
4. 需要细线、端点、文字、颜色或几何判断时保留原始细节；不要为了省 Token 统一降清晰度。优先一次看整图，再只检查结论需要的局部。
5. 本轮结束时输出一张“视觉结果卡”：轮次与基线、逐步骤结论、关键可观察事实、证据 ID/原件定位、冲突与不确定项、建议总指挥最小回读的原图、未解决问题、失效条件。然后停在等待总指挥读取结果卡，不接管调度。
```

操作：截图只发到该观察任务；结束后把“视觉结果卡正文或稳定路径”发给总指挥，不要再次上传整批原图。目的：让原图只停留在短寿命观察任务中，避免长期累积在总指挥聊天。预期结果：总指挥能够仅凭结果卡继续记录、判断和修复；只有结果卡冲突、信息不足或触发安全/专业复核时才点名回查最少原图。异常处理：如果观察任务无法读取测试包、基线不一致、结果卡无法写入或图片证据与步骤无法对应，停止该轮并把错误文字和受影响步骤 ID 发给总指挥。

已经上传到总指挥聊天的旧图片不能由这套流程追溯移除。当前轮尚无可靠断点时先完成最小结案，不为迁移而丢失未持久化反馈；从下一个稳定大轮次开始使用观察任务。

### 3. 发布归属预审、正式发布候选或紧急发布豁免

想先核对现有 Issue/PR 归属时选择“发布归属预审”；自动验证和人工表现都达到可接受状态时选择“正式发布候选审查”；只有时间或业务确实紧急、你决定不完成原定人工验收并愿意承担已知剩余风险时，才选择“紧急发布豁免审查”。紧急豁免不是“验收通过”，不能绕过 Required Checks、有效 Review、权限、安全、隐私或平台保护。

```text
请按场景 2B 执行“既有 Issue/PR 归属发布”的对抗式审查和事实回读。

发布审查阶段：【发布归属预审（允许人工回归未完成） / 正式发布候选审查（自动验证和人工表现均已接受） / 紧急发布豁免审查（明确放弃未完成的人工验收并接受风险）】
紧急豁免原因：【不适用 / 时间或业务紧急原因】(示例：必须在今天完成团队集成，无法继续安排原定人工回归)
未完成的人工验收与风险接受：【不适用 / 逐项列出未完成验收，并明确接受这些未验证表现进入本次发布】(示例：未完成真实素材的最终交互回归；接受仍可能存在局部视觉或交互缺陷，但不得宣称已完成专业验收)
发布对象策略：【只使用现有 Issue 和现有 PR，禁止新建 / 允许在没有可靠载体时建议新建，但本轮仍不创建】
本地候选范围：【自动读取当前安全存档分支尚未远端交付的全部本地 Commit 和已跟踪修改 / 指定 Commit 范围】(示例：自动读取；或 abc123..def456)
明确排除：【无 / 不得进入发布范围的目录、文件或工作包】(示例：本地截图、浏览器会话目录和个人证据记录)
远端动作授权：【先生成执行卡并等待确认 / 只执行 Push / 执行 Push、更新现有 PR、请求 Review并等待 / 本消息同时最终确认下方精确终态动作】
精确终态动作：【无 / 逐项写明允许的 Auto-merge、Approve、Merge、Close 及适用条件】(示例：Required Checks 全绿且取得仓库要求的有效批准后，允许现有 PR Auto-merge；禁止管理员绕过、Force Push 和直接写默认分支)

本轮事实回读的目的不是重复验收代码，而是确定每一部分代码的正确远端载体和执行门禁。请读取最新默认分支、现有 Issue/PR、Base/Head、PR 作者与责任、分支可写性、继承 Review、未闭环意见、冲突、Checks/Actions、Auto-merge 和当前账号权限；同时核对本地存档分支、完整 Commit 链、未提交/未跟踪内容、测试和人工验收证据。若选择预审，必须如实列出尚未完成的发布门禁，并把执行卡标成“草案、不可执行”；若选择正式审查但证据不足且没有有效紧急豁免，自动降级为预审。若选择紧急豁免，必须把原因、未完成验收、风险接受、回滚/停止条件写入执行卡和目标 PR；不得把未完成写成已通过，也不得豁免仓库或平台硬门禁。

建立“发布归属矩阵”：以本地 Commit、文件或必要的代码块为最小候选单元，逐项映射到现有 Issue 和实际承载代码的现有 PR Head，写明匹配依据、置信度、重叠/冲突和不匹配项。标题、标签、编号相近或文件名相同不能单独证明归属。一个 Commit 可以拆到多个 PR，多个 Commit 也可以合入同一 PR；Issue 只记录需求/责任，代码必须进入对应 PR Head。

保护当前存档分支和 Commit 链，不得 Force Push、Reset、重写或把拆包结果覆盖回唯一存档。若需要多目标发布，请为每个目标 PR 从回读后锁定的最新 Head 建立独立本地 worktree/分支；整颗 Commit 归属明确时可计划 Cherry-pick，跨对象时按文件或代码块移植并生成目标分支的新 Commit。此阶段只规划，不实际改写本地历史。

若发布对象策略禁止新建，而某部分没有可靠匹配的现有 PR、目标 PR 已关闭/合并且不可继续、分支不可写、责任冲突或正在发生同范围并行修改，请把该部分保留在本地并标记阻断，不得勉强归入相似对象，也不得创建新的 Issue 或 PR。

请输出：本地最终变更与准确 Commit 范围、发布归属矩阵、默认分支和各目标 PR 的最新 Base/Head、责任/权限/Review/Checks/冲突状态、验证与人工证据、未匹配或暂留本地项、剩余风险，以及一张最小远端发布执行卡；选择预审时必须明确称为“不可执行草案”。执行卡必须把本地拆分准备、Push、更新现有 PR 正文、Issue/PR Comment、回应 Review、请求 Review、CI 重跑、启用 Auto-merge、Approve、Merge、Close 分别列出目标、动作、授权状态和失效条件；没有必要的动作明确写“无”。

如果“远端动作授权”选择等待确认，先不要执行任何远端写入，也不要执行会改变本地历史的拆分/移植；等我确认后只执行获批动作。如果选择“本消息同时最终确认下方精确终态动作”，且对象、动作、条件和禁止项已经明确，则本消息同时构成远端执行卡确认，可以连续执行本地移植、Push、更新现有 PR、请求 Review，并在精确条件满足后执行已列终态动作。未列出的 Merge、Approve、Close、Auto-merge、Issue/PR 新建或部署一律未授权；Base/Head、范围、权限、Required Checks 或 Review 门禁漂移时只暂停受影响动作，不得绕过。
```

怎样使用这个模板：通常只需选择一个“发布审查阶段”，填写你真正决定的业务字段；Commit、Base/Head、PR 编号和执行卡 ID 由 AI 回读，不需要你查。正常发布把紧急字段填“不适用”；紧急发布必须把“为什么跳过、跳过什么、接受什么风险、最终允许做到哪里”四件事写清楚。想一条消息直接执行时，必须选择“本消息同时最终确认”并把终态条件写完整，不能只写“自动提交、合并等等”。

场景切换的判断标准：

1. 远端仅有需求讨论，不存在实现分支、PR、明确负责人或未闭环 Review 时，可以把需求作为证据后按子场景 A 建立新的本地实现包。
2. 已存在实现 PR、活跃分支、明确负责人、Review request、未闭环意见或队友新提交时，必须使用子场景 B 或拆分混合场景。
3. 对象由使用者本人负责时，可以在隔离分支本地续作；是否继续更新原 PR，要等发布前核对当前 Head、分支权限和团队规则。
4. 对象由他人负责时，AI 先判断直接修复是否比交棒更高效且不会扰乱并行开发。没有可核验交接或明确授权时，不得直接写入对方分支；但在团队规则允许、问题明确可复现、没有同范围并行修改且本地执行卡已确认时，可以在本人隔离修复分支上实现并验证候选方案。该动作不改变 Assignee、长期责任或远端权限；发布前再按团队规则选择补丁 PR、交付 Commit 或请原作者吸收。若需要作者专属背景、产品决定或正在发生同范围修改，则交回原作者并说明恢复条件。
5. 当前工作区存在来源不明、与目标混杂或无法恢复基线的修改时，先进入恢复场景：识别归属、保存证据并隔离目标，不能直接开始 A 或 B。
6. 最终发布继续使用现有远端授权规则；本场景不会绕过仓库明确要求的独立 Review、Required Checks、CODEOWNERS 或管理员门禁，也不会把风险建议伪装成仓库硬规则。


## 场景 3A：全量接管协作队列中的 Issue/PR

读取文档：按 `02` 的标题级映射读取 3A/现有 PR/并行章节，并读取远端计划所需的 `09`。

复制发送：

```text
请对【一个或多个目标】(示例：目标模块1，目标模块2)当前协作队列中的 Issue 和 PR 执行全量接管推进。
使用者本人平台账号：【平台账号】
本轮协作成员平台账号：【一个或多个平台账号】(示例：账号1，账号2)
范围补充或限制：【无 / 具体补充或排除范围】(示例：排除仅涉及部署的对象)
本轮实现与 PR 推进责任：【使用者本人优先 / 指定平台账号 / 按对象分别指定 / 由 AI 根据现有责任证据建议】
责任依据或对象例外：【无 / 具体要求】(示例：目标模块由使用者本人优先实现；已有负责人或活跃分支的对象保持原安排)
默认纳入任务规则：目标外任务只有在正式请求使用者本人 Review、直接点名、分配给使用者本人、明确交办，或对使用者本人已有参与作出可核验的直接回复时，才进入正式候选；跨模块弱关联只汇总并等待确认。该规则同时适用于已列和未列的团队成员。
私聊/会议交办：【无 / 原文或可核验摘要】(示例：会议决定由使用者本人处理目标模块，其余成员负责审阅)
协作轮次、规则版本、状态快照、启动截点和任务 ID 由总指挥自动处理，不需要我填写。
若必填占位符仍未替换，立即停止并列出待补字段，不要自行推断。

范围不能只按创建者过滤，也不能因某人出现在协作成员身份卡中就接管其全部任务。按核心规则自动覆盖目标内对象、目标外强参与信号、未闭环旧账、派生对象和最小依赖；使用者本人创建对象上的新协作事件若不命中目标，也必须通过目标外强信号门禁。没有明文回复的代码变化必须核验对象、分支和关联关系。每个正式候选必须写明纳入依据；没有依据时排除。跨模块弱关联对象只做低成本登记并在批量执行卡中单列询问；我未明确选择时不得执行。随后完成依赖分类、堆叠关系和可执行顺序，每个执行对象保持独立状态，单项受阻不得暂停其他可执行对象。

第一阶段只读扫描和必要验证，不创建实现任务、不改产品代码、不执行远端写入。生成一张批量执行卡，逐对象说明建议动作、协作价值、风险、验证、禁止项、失败回退和交棒终点。

收到我一次集中确认后，按授权规则无人值守推进。结束时重新覆盖核账，区分本地与远端结果，输出建议查看顺序、逐编号状态、PR 堆叠树、Issue 依赖树、当前行动方、真实缺口、使用者本人的必须/建议人工操作，以及给本轮协作成员的短交接包。
```

## 场景 3B：协作队列增量推进与覆盖核账

读取文档：按 `02` 的标题级映射读取 3B/现有 PR/并行章节，按需读取 `04` 与 `09`。

复制发送：

```text
请对【一个或多个目标】(示例：目标模块1，目标模块2)执行一次协作队列增量推进与覆盖核账。
使用者本人平台账号：【平台账号】
本轮协作成员平台账号：【一个或多个平台账号】(示例：账号1，账号2)
范围补充或限制：【无 / 具体补充或排除范围】(示例：只复查上轮未闭环对象和新 Review)
本轮实现与 PR 推进责任：【使用者本人优先 / 指定平台账号 / 按对象分别指定 / 由 AI 根据现有责任证据建议】
责任依据或对象例外：【无 / 具体要求】(示例：新增可执行项由使用者本人优先实现；已有负责人保持不变)
默认纳入任务规则：目标外任务只有在正式请求使用者本人 Review、直接点名、分配给使用者本人、明确交办，或对使用者本人已有参与作出可核验的直接回复时，才进入正式候选；跨模块弱关联只汇总并等待确认。该规则同时适用于已列和未列的团队成员。
私聊/会议新增交办：【无 / 原文或可核验摘要】(示例：会议新增要求先处理目标模块的接口问题)
自动识别增量基准并用一句话回显依据；基准不可靠时转为全量推进，不要求我查询或填写历史 ID。若我要指定特殊历史轮次，会在本指令末尾主动说明。

按核心规则只读检查新增/变化对象、既有对象上的新协作事件、未闭环旧账、派生交付物和最小依赖。使用者本人创建且出现本轮协作成员新 Comment、Review、Commit/Push、关联交付或状态变化的对象必须经过参与信号判断；只有能证明本轮需要受理或回应时才纳入正式候选。扫描入口中发现的跨模块弱关联对象单列为默认排除的观察项；我未明确点名确认时不得执行。增量推进不是只扫描新编号，也不重复执行已有有效回执的动作。

先生成最小批量执行卡；集中确认后只执行卡内动作，单项受阻时继续其他独立对象。每轮内部必须复用本轮扫描和执行结果完成轻量结束核账，回读受影响对象、未闭环旧账和派生对象，核对四类差集并保存下一可靠截点；不要另建固定的“每日核账”步骤，也不要因此重新通读全部历史、附件或 Diff。只有任务中断、截点不可靠、数量矛盾、范围变化、怀疑遗漏或正式交接时，才升级为加强核账或全量推进。结束时输出本轮变化、四类差集、PR 堆叠树、Issue 依赖树、当前行动方、当前可执行项、外部门禁、使用者本人的必须/建议人工操作和下一可靠截点。
```

## 场景 4：汇报进展或最新拓扑

读取文档：`02-总指挥核心规则.md`

复制发送：

```text
请只做当前项目进展和拓扑汇报。
只刷新与当前目标有关的最小事实，不重复读取全部历史。
输出：目标版本、工作项关系、当前波次、已完成证据、真实阻断、下一责任人、下一步。
不要创建任务，不修改代码，不执行远端写操作。
```

## 场景 4A：汇总并批量反馈人工操作

读取文档：`03-专项任务卡模板.md`

场景 2A、2B、3A、3B 已默认输出人工操作。只有需要重新汇总、补看或怀疑遗漏时，复制发送：

```text
请汇总当前轮次中所有需要【使用者本人】参与的事项，并生成一份可直接照做的批量人工操作清单。
先排除 AI 能自动完成的步骤；其他团队成员、领域专家或管理员的事项只列为外部门禁，不混入我的操作清单。
按优先级和依赖顺序排列；相同操作可以合并，但必须保留每个 Issue、PR 或任务的独立状态，并使用“对象类型+编号”避免同号混淆。
每组操作说明：关联对象、参与类型、是否阻断、推荐方案、最少步骤、目的、预期结果、异常时需要的截图/日志/现象，以及完成后可直接复制的批量反馈模板。
已能从项目、平台或状态索引读取的信息请自动预填，不要要求我查找技术 ID 或重复填写环境信息。
```

完成一批人工操作后，复制发送：

```text
以下是本轮人工操作反馈，请读取并更新每个对象的独立状态；单项失败、未执行或证据不足不得被推定为通过，也不得暂停其他独立对象。

证据附件根目录：【无 / 本地绝对路径】(Windows 示例：C:\path\to\evidence；macOS/Linux 示例：/path/to/evidence)
共同说明：【无 / AI 无法从附件和项目中判断的补充】(示例：所有截图均来自同一次测试)

【Issue / PR / 任务 + 编号】
- 结果：【通过 / 失败 / 部分通过 / 未执行】
- 实际观察：【无补充 / 观察到的现象】(示例：点击后页面无响应)
- 证据位置：【无 / 对应子目录或文件名】(示例：issue-1/error.png)
- 额外说明：【无 / AI 无法自动判断的信息】(示例：本次使用移动网络测试)

请自动核对附件与编号的映射、判断证据是否足够、更新阻断和恢复条件，并继续所有能够自动推进的事项。只有确实还需要我参与时，才重新生成最小人工操作包和可复制提示词。
```

对象很多时可以把重复块改成紧凑表格：`对象｜结果｜实际观察｜证据位置｜额外说明`。AI 应预填对象列表，使用者本人只补观察结果；没有反馈的对象保持“未反馈”，不得按通过处理。

附件目录建议：每一批人工操作建立一个独立根目录，例如 `人工反馈/<YYYYMMDD>-本轮/`，再按 `issue-<NUMBER>`、`pr-<NUMBER>`、`task-<TASK_ID>` 建立子目录；多个对象共用的证据放在 `shared`，并在反馈中写明覆盖哪些对象。文件可用 `01-before.png`、`02-after.png`、`error.png`、`notes.txt` 等简短名称。只需把本批次根目录的绝对路径发给 AI，不必逐个上传；AI 先建立或读取本轮清单，只打开与当前步骤和结论有关的文件，不默认递归分析整批旧附件。

不要只用数字命名子目录，否则 Issue 和 PR 同号时会冲突。含个人数据、受监管数据、账号、密钥或其他敏感信息的附件应先脱敏，默认放在仓库外；确需放入项目目录时只能使用项目已忽略的调试目录，并确认不会被 Git 提交。系统临时目录中的文件可能自行失效，AI 必须在清单中标记“临时来源”，但不得擅自把含人脸材料复制进仓库。附件只能作为证据，不能因为文件存在就自动判定测试通过。

## 场景 4B：在新任务中解释上一轮某个对象

总指挥提醒适合分流时，打开一个新任务并复制下面提示词；也可以明确要求当前 AI 创建新任务。这样做的目的是让原总指挥继续推进主线，同时让新任务只读取目标对象的短回执和当前事实。

```text
请从项目总指挥入口恢复与【对象类型 + 编号】有关的最近可靠执行回执和 GitHub 当前事实，只读解释：上一轮实际做了什么、为什么这样做、哪些替代动作没有采用及原因、原结论是否因新事实失效、当前行动方和下一步。
只读取该对象的决策回执、证据指针和验证结论；不要重读无关聊天、附件、历史或 Diff。不要修改代码、中央状态或远端平台。
如果找不到完整的可迁移决策回执，明确告诉我回到原总指挥补录，不要根据零散历史猜测。
```

预期结果：新任务能独立解释该对象，又不接管主线。若它报告回执缺失、事实冲突或截点失效，把报告发回原总指挥补录或重新核验；不要让两个任务同时写中央状态或执行同一对象。

## 场景 5：项目状态或阶段目标发生变化

读取文档：`04-状态、目标变更与交接规范.md`

复制发送：

```text
项目发生了以下变化：
变化内容：【AI 无法从项目或平台自动发现的变化】
非平台来源：【无 / 会议、私聊或人工决定的可核验摘要】(示例：会议决定暂停旧方案并采用新接口)
请判断它影响哪些目标、Issue/PR、专项任务和验证结论。
暂停或标记失效的任务，保留原因和证据；只重算受影响子图。
不要删除历史，不要自动吸收未确认的新目标，不执行远端写操作。
最后汇报：继续项、暂停项、被替代项、需重测项和需要我确认的决定。
```

## 场景 6：交接给下一任总指挥

读取文档：`04-状态、目标变更与交接规范.md`、`07-总指挥交接记录模板.md`

复制发送：

```text
请准备总指挥交接。
先只读检查当前是否存在未核验的高风险动作、未记录的任务断点、重复调度或事实冲突。
先按规则健康检查并在需要时压缩当前进度视图；不得删除原始证据或让历史失去回查入口。
生成一份便携交接快照：事实截点、当前目标、代码与远端基线、活跃任务、阻断、未提交成果、下一责任人、验证证据入口、禁止项、恢复条件、失效条件和第一次只读动作；同时记录 `总指挥轻量交接启动配置.md` 的路径/版本/SHA-256，以及 02、04、07、09、10 的来源指纹。正文只保留聚合结论和稳定指针，不复制历史聊天、完整 Git 清单、Diff、日志或旧截图。
未通过稳定截点检查前，不停止旧总指挥，不启动新总指挥调度。
```

## 场景 6A：新总指挥接收正常交接

适用情况：旧总指挥仍可用，并且已经按场景 6 生成了通过稳定检查的正式交接记录或便携快照。它不同于场景 1A：场景 1A 用于旧任务不可读、不可写或根本没有可靠交接材料的灾难恢复；场景 6A 用于有明确交接记录的正常换任。

在新账号的目标项目中建立一个新任务，把交接记录作为附件提供，或把它的本地绝对路径填入下方提示词。不要只说“你接着做”，也不要在第一次消息中授权它继续旧队列。

读取文档：交接记录指定的项目规则和状态入口，以及 `总指挥轻量交接启动配置.md`。新任务必须完整读取该配置并建立自己的加载记录；配置和 02、04、07、09、10 的来源指纹全部匹配时，候选阶段不再全文读取五份编号规则。任一指纹异常时只展开受影响规则；配置缺失、来源清单不完整或影响范围不明时退回完整读取。

第一次发送：

```text
我现在明确指定你为本项目的“新总指挥候选”。请读取下面的正式交接记录，并执行一次正常交接的第一次只读接管：
交接记录或便携快照：【本地绝对路径 / 已附附件】

本轮只做接管核验，不开始新的全量、增量或专项调度。先按交接记录定位项目入口、当前进度视图、AI 状态索引和第二代规则根目录，并由你自己完整读取 `总指挥轻量交接启动配置.md`、核对其 SHA-256 及 02、04、07、09、10 的来源指纹；只有异常项才展开对应编号规则，无法确定影响范围时退回完整读取。不得继承旧任务的规则已读状态、远端授权、运行中进程、完成声明或专业结论。

只读核对：交接快照是否仍在有效期内、旧总指挥世代与单写者状态、当前目标和精确断点、工作区/分支/worktree/未提交修改、代码与 Git/GitHub 基线、验证证据入口、阻断、禁止项、恢复条件和失效条件。默认只输出分支、Head、状态计数、聚合指纹和 PASS/WARN/FAIL；不读取完整旧任务、Git 路径清单、Diff、日志或冷截图，只有冲突项才最小展开。已有完成凭证的任务不得重复执行；发现事实漂移时只标记受影响范围、证据和最小补救动作。

核对完成后，先回复：恢复置信度、已确认事实、待确认或冲突项、交接是否达到可切换条件、拟登记的新总指挥世代/任务 ID/接管截点。事实可靠时可以写入“候选接管截点”，但在我明确确认切换前，不得终止旧总指挥，不得取得中央调度权，也不得执行产品代码或远端写操作。
```

预期结果：新任务应明确说自己当前只是“新总指挥候选”，给出聚合核验依据和候选接管截点，并等待你先停止或归档旧总指挥、再做最后一次切换确认。若交接记录缺失、规则指纹变化、Git/平台事实冲突或存在另一个调度者，它必须保持只读并列出最小缺口。

当新任务明确报告“已达到可切换条件”后，再发送：

```text
我已停止或归档旧总指挥，并确认将本项目总指挥调度权切换给你。请先最小复核轻量启动配置/来源指纹、中央世代与单写者、候选截点失效条件、工作区聚合指纹和当前远端基线；仍可靠时，把候选接管截点升级为正式接管截点，登记新的唯一总指挥世代和当前任务 ID，并把旧总指挥标记为已停止调度但保留全部历史。回读更新后的项目入口、当前进度视图和 AI 状态索引，确认单写者唯一、旧授权未继承，然后只从交接记录中的精确断点继续。未包含在我当前明确指令里的产品代码修改和远端写操作仍未授权。
```

只有旧总指挥已由操作者停止或归档、第二段确认发出、切换前最小复核通过且中央状态回读一致后，换任才算完成。第一段只建立候选身份和只读接管，不转移调度权；候选不得自行终止旧任务。

如果只是临时交给其他 AI 执行一段任务、不转移总指挥权限，把生成的便携快照连同下面提示词发给它：

```text
请把附件视为有失效条件的只读交接快照，先核对项目规则、当前代码和远端事实，再处理我明确交给你的范围。
当前总指挥仍是中央状态的唯一写入者；不要直接覆盖中央进度、拓扑或状态索引。
你可以更新收到的便携交接快照副本。结束时返回：更新后的快照、实际完成、代码/远端变化与回执、验证、未完成项、阻断、下一责任人，以及可由总指挥核验写回的状态增量。
任何远端写操作仍须遵守当前项目规则和本轮精确授权。
```

## 场景 7：复盘最近一批任务

读取文档：`06-复盘与优化规则.md`

复制发送：

```text
请复盘最近一批任务，只基于实际工具调用、任务回执、验证结果和人工介入记录。
指出哪些步骤消耗了不必要的 Token、哪些任务拆分或模型选择不合适、哪里产生重复读取或重复汇报。
给出最多 3 条可执行改进，并说明预计减少的读取、轮次或人工确认。
不要因为复盘而修改项目代码或远端状态。
```

## 场景 8：暂停本轮工作

读取文档：`04-状态、目标变更与交接规范.md`

复制发送：

```text
请安全暂停当前 Goal。
先保存当前范围快照、任务断点、未提交修改、验证结果、阻断和恢复条件。
不要删除任务、分支、worktree 或历史记录；不要执行远端写操作。
最后给出下一次恢复时应先读取的文档和第一步只读检查。
```

## 远端协作授权：默认由 AI 生成

```text
请根据只读扫描结果自动生成一张最小批量执行卡，自动填写卡 ID、精确对象、当前 Base/Head、允许动作、分支、验证、禁止项、有效期、失效条件和失败回退。
把最终确认文本一并生成给我复制；我只负责确认、拒绝，或补充 AI 无法决定的限制。
```

确认文本可直接写“我确认刚刚生成的批量执行卡，按卡内边界执行”。只有同时存在多张待确认卡时，AI 才在文本中自动带入对应卡 ID；不得要求使用者本人手工查找。对象必须扫描后才能确定时，不填写猜测编号。

批量执行卡的正文必须让非专业使用者也能判断，固定先写：AI 推荐、为什么、最坏可能结果、仓库硬保护、授权后会自动做什么、明确不会做什么、什么情况下自动跳过。编号、Base/Head、分支和精确 Review 内容放在附录，不用它们替代人话风险说明。

只有人类、项目规则或平台真实授予的权限才有效。AI 的自动预填和历史授权记录不能替代本轮最终确认。
