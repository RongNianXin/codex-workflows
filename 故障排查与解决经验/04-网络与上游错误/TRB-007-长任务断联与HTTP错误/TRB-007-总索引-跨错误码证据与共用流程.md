# CC Switch 长任务断联与 400、401、404、429、502、503、504 快速处理

| 字段 | 当前记录 |
| --- | --- |
| 故障编号 | `TRB-007` |
| 解决状态 | **部分解决（分流与恢复流程可用；上游故障无法本地消除）** |
| 工具状态 | 无配套工具 |
| 最后核验 | 2026-09-10（补充heartbeat工具结果关联失败400；历史样本未重跑） |
| 证据边界 | 长度400与工具结果配对400分别记录；heartbeat样本来自已核验报告指纹的来源方取证，本窗口只回读报告与官方协议，未读取该样本原始日志或取得转换前后请求体，根因与修复未验证 |

> 本文是跨案例处置指南。它不能证明某个 5xx 的唯一根因，也不能保证切换线路后原任务可以继续。

## 按错误码直达

- `400` 对话过长：[HTTP-400-TRB-007-对话过长](HTTP-400-TRB-007-对话过长.md)
- `400` 工具调用配对：[HTTP-400-TRB-007-工具调用配对](HTTP-400-TRB-007-工具调用配对.md)
- `401` 凭据或授权：[HTTP-401-TRB-007-凭据或授权拒绝](HTTP-401-TRB-007-凭据或授权拒绝.md)
- `404` 模型不支持：[HTTP-404-TRB-007-模型不支持](HTTP-404-TRB-007-模型不支持.md)
- `429` 模型线路限流：[HTTP-429-TRB-007-模型线路限流](HTTP-429-TRB-007-模型线路限流.md)
- `502` 上游网关：[HTTP-502-TRB-007-上游网关错误](HTTP-502-TRB-007-上游网关错误.md)
- `503` 服务暂不可用：[HTTP-503-TRB-007-服务暂不可用](HTTP-503-TRB-007-服务暂不可用.md)
- `504` 网关超时与断流：[HTTP-504-TRB-007-网关超时与断流](HTTP-504-TRB-007-网关超时与断流.md)

本页保留跨错误码证据、共用安全流程、固定探针和历史复盘；按单一错误码处理时优先进入上面的独立文件。

## 一、文档目的

本文根据两类已脱敏的长任务断联案例整理，目标是解决两个长期问题：

1. 使用 CC Switch 切换账号或中转站后，借助项目状态记录或交接快照可靠恢复任务进度；这不承诺原聊天历史或隐藏 reasoning 状态可以跨 Provider 兼容。
2. 再遇到明确对话过长的400、401、模型不支持404、502、503、504时，按错误正文缩小账号、模型映射、网络、代理或任务历史的排查范围，不把状态码当作唯一根因。

本文不要求放弃 CC Switch。CC Switch 仍适合统一管理账号和一键切换；需要改变的是长任务的状态管理和切换方法。

## 二、两类案例的已确认事实

### 1. 故障并非单纯“额度不足”

本次先后出现了两类不同故障：

- 旧 provider 返回 `401 Invalid API key`，说明该凭据已被上游拒绝。
- 原长任务后续的关键错误为：

```text
input[17].encrypted_content missing_required_parameter
```

第二类错误是 Responses 历史重放或代理转换时缺少必需字段，不是典型的额度耗尽提示。

第三方中转可能重新包装状态码，因此不能只看 `401`、`502` 等数字；必须同时看错误正文里的 `cause`、`upstream_status` 和参数名称。

### 2. 本地会话文件没有发现对应字段损坏

离线检查结果：

- 会话文件规模较大，包含多轮 JSONL 历史。
- 成功请求包含较长的输入历史和多次上下文 compaction。
- 已抽查的顶层 reasoning item 均有非空 `encrypted_content`。
- 最新 compaction replacement history 也保留了加密内容。

因此，现有证据不支持“磁盘 JSONL 本体丢字段”。更可能的故障位置是失败回合 rollback 之后的请求组装、历史裁剪或 CC Switch / 上游的格式转换层。

### 3. 直接换中转站不能修复结构化历史错误

如果原线程已经在失败回合中形成不兼容或不完整的请求历史，切换到另一个健康 provider 后，原线程仍可能继续发送同一份坏请求。本节只解释包含密文、字段转换或分页谱系信息的结构化错误，不适用于只显示上游暂时不可用的纯 502。

这解释了为什么：

- 新建短对话可以回复。
- 旧的长任务仍然失败。
- 再换一个中转站也不一定恢复。

### 4. 历史案例曾用 Fork 绕开失败活动回合

案例中从原任务建立了同目录 fork：

```text
<FORK_TASK_ID>
```

恢复任务只继承已经完成的历史，不包含失败中的活动回合。只读恢复握手成功，并正确复述了既有断点，没有再次出现 502 或 `encrypted_content` 错误。

这只证明 Fork 在该案例中绕开了失败活动回合，不能把它推广成默认恢复方式。当前规则要求先从项目现有状态记录或交接快照恢复；只有操作者明确要求继承源任务的已完成聊天历史时，才使用 Fork。

### 5. 纯上游 502 是另一类故障

后续案例的错误正文没有字段缺失、transform 或分页谱系信息，而是：

```text
unexpected status 502 Bad Gateway: CC Switch local proxy failed while handling Codex endpoint /responses. Provider: <PROVIDER_A>; model: <SOL_MODEL>; upstream_status: HTTP 502; cause: Upstream service temporarily unavailable, url: http://127.0.0.1:<PORT>/v1/responses
```

其中 `127.0.0.1:<PORT>` 是用户电脑上的 CC Switch 本地代理地址，不是中转站服务器地址。该错误只证明 CC Switch 转发 `/responses` 时收到或生成了上游 502；客户端证据不能继续区分账号池、调度节点、认证刷新、容量、超时或协议转换中的哪一项失败。

日志显示，真实长任务先成功读取材料和执行部分工具，下一轮 `/responses` 才报 502。多数同类 502 很快返回且没有输入、输出 Token；同一时间段前后也存在成功请求。这与“上游短时不可用或调度失败”一致，但最终根因仍需要中转站服务端日志确认。

### 6. 502 后的同一时段对照

案例中 CC Switch 自动故障转移始终关闭，因此下列请求没有被自动切换到其他备用 Provider。一次 502 复现后，在较短时间内进行了以下只读对照：

| 对照 | 结果 | 能说明什么 | 不能说明什么 |
| --- | --- | --- | --- |
| Provider A，干净窗口，固定文本 | 成功 | 账号和代理没有持续失效 | 复杂请求已经恢复 |
| Provider A，干净窗口，一次工具及结果续轮 | 成功 | 基础工具续轮可用 | 所有工具链和长上下文都可用 |
| Provider A，原长任务窗口，固定文本 | 成功 | 原线程没有永久损坏 | 原复杂任务再次执行一定成功 |
| Provider B，固定文本及工具续轮 | 成功，但模型档位不同 | 另一条中转链路当时可用 | 与 Provider A 构成严格同模型对照 |
| Provider B，同一复杂任务 | 使用相同模型家族成功完成 | 任务复杂度本身不是充分触发条件 | 两个 Provider 的账号池和节点完全相同 |

另一个独立的复杂任务在 Provider A、同一 Sol 模型和极高推理设置下连续运行约 10 分钟并完整成功。这个结果进一步说明，运行时间、任务复杂度或极高推理本身都不是 502 的充分条件。

这组证据推翻了两个过强判断：

1. 不能因为干净探针成功，就直接认定原线程历史损坏。
2. 不能因为复杂长任务曾失败，就认定长任务、工具调用或高推理必然触发 502。

更谨慎的结论是：Provider A 的 502 具有间歇性；长任务请求次数更多，可能更容易撞上短时故障，但它只是风险放大因素，尚未证明是根因。

### 7. Astra 404 与 Sol 502 必须分开

Provider A 使用 Astra 的多个推理档位时，均返回：

```text
unexpected status 404 Not Found: CC Switch local proxy failed while handling Codex endpoint /responses. Provider: <PROVIDER_A>; model: <ASTRA_MODEL>; upstream_status: HTTP 404; cause: Model "<ASTRA_MODEL>" is not supported by any configured account in this group, url: http://127.0.0.1:<PORT>/v1/responses
```

历史短请求中，切回 Sol 后同一账号和任务可以继续响应。Provider B 也曾使用 Astra 完成长回复，并报告五类 P 测试成功；窗口之间的真实工具上下文继承仍需原始事件核对。因此：

- Astra 404 已明确指向 Provider A 的账号组或模型映射不支持该模型；改变推理等级无效。
- Sol 502 是间歇性的上游可用性问题，当前没有唯一根因。
- 两种错误的状态码、`cause` 和修复责任不同，不能合并为“模型太复杂”或“CC Switch 不支持 Astra”。

### 8. `model_provider = custom` 不是充分根因

据操作者报告，发生 502 的中转配置与稳定使用的官方账号配置都曾使用 `custom`；三份原配置文本缺失，尚未逐字段复核。现有证据不支持把 `custom` 单独认定为 502 的根因，也不能据此排除其他配置问题。总路由开关还会改变实际数据路径，所以比较配置时必须同时记录请求是否经过 CC Switch 本地路由、目标地址、模型映射和账号组，不能只比较 `custom` / `OpenAI` 标签。

### 9. 回滚后错误消失不证明回滚修复了 502

历史迁移回滚可以撤销会话文件或分页谱系改动，因此能够解释迁移后出现的结构化红色错误为什么消失；它不能修复中转站上游短时不可用。回滚后 502 同时消失，可能只是上游在操作期间恢复、客户端重新发起了新请求，或线路状态发生变化。没有回滚前后同一请求和服务端日志，不能建立因果关系。

### 10. 修复声明与修复后的新 502 分开记录

服务方声明已修复非 ASCII 请求头转发、补齐模型专线映射，并报告 213 项测试通过。这是外部声明，不是客户端独立验收。中文提示词主要进入请求正文，成功处理中文不能单独验证 Header 转发；需要服务端确认实际请求命中对应路径，不应为测试而公开请求头或凭据。

修复声明及本机进程重启之后，又观察到以下不同的错误：

```text
unexpected status 502 Bad Gateway: CC Switch local proxy failed while handling Codex endpoint /responses. Provider: <PROVIDER_A>; model: <MODEL_NAME>; cause: 转发失败: 上游连接失败: error sending request, url: http://127.0.0.1:<PORT>/v1/responses
```

本次没有 `upstream_status` 字段，不能补写“已收到上游 HTTP 502”。`cause` 指向转发时连接或发送请求失败；与旧 `upstream_status: HTTP 502; cause: Upstream service temporarily unavailable` 是否同根因仍待服务端日志确认。它证明修复后仍出现502，不证明服务方此前的特定修复无效，也不支持把全部问题归咎于中转站。

### 11. 换本机代理节点后只有短时成功证据

操作者更换本机代理节点后，后续工具调用及一个新审查样本短时成功。CC Switch 实际出站是否经过该节点仍待确认；直连或使用其他网络路径时，节点切换可能无关。切换前后必须分成不同观察区间，短时成功不能确认代理根因或永久稳定。

重启是可选的新增实验变量，不是每次换节点必须执行的产品要求。只有需要重新加载配置且任务已到安全停止点时，才另行安排重启，并记录新的观察起点；不要强迫中断运行中的写入。进程创建时间可核对是否重启，自动登录只说明认证状态被保留，不证明没有重启；重启也不等于清缓存、清凭据或清历史。

### 12. 明确对话过长的 400 应优先恢复工作现场

同一旧会话在不同模型及最小握手下均出现：

```text
upstream_status: HTTP 400; cause: This conversation has grown too long to continue. Please start a new chat session to keep working.
```

错误语义明确为该会话无法在当前链路继续；限制由中转、适配层、账号组还是最终上游实施仍待确认，不能宣称已排除中转影响。停止向该旧会话反复试探，从已有项目状态和交接材料恢复；旧会话不能继续时，不要求它必须先成功输出交接。创建新任务仍按当前操作者授权和工作流执行。

本地 JSONL 大小、压缩次数及工具估计的上下文比例不是服务端实际请求长度，也不是统一阈值；历史更大的本地文件仍可运行并不矛盾。交接评估应以实际失败事件中的明确长度拒绝覆盖一般评分，但不能把所有400或用户引用的错误当作真实失败。本轮只记录该工具需求，没有修改其代码。

旧会话握手只有投递入口接受，生成随后400失败，不构成双向通信。新会话成功只证明新样本。消息已入站却空回复或漏 ACK 时转 [TRB-006：AI任务间消息不回复](../../03-跨任务通信/TRB-006-AI任务间消息不回复-排查与预防.md)，不要与长度400合并。

#### 新增核对：同一错误签名，不等于同一根因

另一长任务在跨任务协作期间，发送接口接受消息后，平台两次可观察的失败回合均返回同一长度400，未提供助手回复；操作者另报告已无法继续发消息。它与上面的历史样本错误签名一致，但初始采集只核对到一条模型/Provider路线，没有复跑历史多模型或干净任务对照。两次失败回合不等于两次人工重试，也不证明所有旧轮次都未执行。

后续同一旧任务恢复过一次短文本回复，平台回读确认该回合成功且没有错误。操作者报告当时切到了由同一本地账号管理工具接入、绕过中转的官方账号；成功回合的实际模型、请求长度、压缩和出站路线未独立核验。之后操作者报告官方额度耗尽、已切回中转，又观察到标记中转路线的同一句长度400。额度耗尽是操作者说明切换原因，不是长度400的已证实原因。这些观察提高了路线差异的核查优先级，但不是严格单变量对照，也不证明工具续轮、双向通信或业务已恢复。

| 判断 | 当前证据与缺口 |
| --- | --- |
| 当前路径拒绝继续处理该会话 | 平台实际失败事件含 `invalid_request_error`、`cc_switch_upstream_error`、`upstream_status: 400` 及明确长度正文；不是仅在消息中引用报错。 |
| 是否只是本机网络故障 | 明确的应用层拒绝与普通超时、断连不同，优先检查会话限制和请求组装；错误经过本地代理包装，仍需出站日志确认，不能断言网络已被完全排除。 |
| 是否真的达到长度限制 | 可能成立；需要实际发送的输入/Token计数、有效模型及限制、压缩前后记录。短新消息仍可能携带历史，本地日志大小不能替代请求长度。 |
| 是否中转兼容问题 | 可以请服务方核查，但尚未证实。重点确认错误由哪层产生、有无重复追加历史、压缩状态是否保留、模型映射及会话限制是否正确；不能从 Provider 标签或旧修复声明定因。 |

**当前恢复与防复发：**确认这种明确拒绝后，停止在故障路线向原任务追加业务、ACK查询和仅为确认而发送的状态消息；状态保存在健康窗口即可，避免每次投递又触发一个失败回合。有另一路线短回复成功时，不宣称旧任务必须重建；能否继续仍取决于该路线可用性和获准的有限验收。没有可用路线时，保留旧任务及未提交成果，在授权内让新任务按场景6B只读恢复原项目、原角色和可靠断点；旧任务不能回复时不要求它先写交接。恢复工作不等于修好原会话；Fork会继承历史，不能保证避开长度限制。新任务准备好后仍须核对未完成动作和唯一写入者。

OpenAI 的[上下文说明](https://developers.openai.com/api/docs/guides/conversation-state#managing-the-context-window)区分实际请求的上下文窗口与聊天文件；[压缩文档](https://developers.openai.com/api/docs/guides/compaction)提供服务器压缩及独立压缩接口，并要求独立压缩的输入仍在模型窗口内。它们不能证明某个桌面版本、账号路线或中转已正确接入，也不能保证报错后输入一条“压缩”就能恢复。不要为绕过限制删除历史、密文或擅改配置。

**向服务方取证：**先提供时间和时区、模型、脱敏错误及本次是否有线路对照；请其回传请求关联号、实际上游/有效模型、原始状态与错误、计数和限制依据、历史组装及压缩结果。无需发送项目内容、照片、凭据或完整会话。只有后续明确获准，才用无业务副作用的独立样本做单变量对照；不同账号、模型、路由或新旧历史同时变化，只能说明路径差异，不能独立锁定中转故障。

### 13. 503 前已写入的结果必须保留

另一个失败回合在503前已执行命令并修改3个文件，且该事件发生在后续进程重启之前，不能用于断言重启后仍503。恢复先核对已完成、结果未知、尚未开始的动作，仅续未完成部分；缺少最终回复不等于此前没有副作用。

### 14. heartbeat 后持续出现工具结果配对400

**状态：关联失败现象已有来源证据，责任层待核，未修复。** 本样本与第12项的对话过长400分开，不能只按状态码套用长度限制解释。

来源方在Windows环境的既有会话中观察到：heartbeat被保存为内部 `function_call_output`，有结果自身的 `id`、`name=automation_update`、`namespace=codex_app` 和唤醒正文，但没有 `call_id` 键。约4秒后代理报告400；随后两次输入仍报同一结果标识，其中一次只是普通问候。以下为匿名错误签名：

```text
No tool call found for function call output with call_id <RESULT_ID>.
type: invalid_request_error
code: cc_switch_upstream_error
param: input
```

本窗口已核验来源报告的大小与SHA-256并回读正文。来源方报告在故障项之前限定的日志范围内检查了115个函数/自定义工具调用记录，未找到匹配；本窗口未重跑该扫描，不能扩成全部历史不存在对应调用。当前安装项为CC Switch 3.20.2，会话元数据 `cli_version` 为0.153.4；这些是来源方当前取证字段，不证明故障当时运行二进制版本。三次同会话失败不是三次独立复现，也不能估计触发概率。

[OpenAI函数调用文档](https://developers.openai.com/api/docs/guides/function-calling)要求工具结果引用具体调用，示例使用对应的 `call_id`。但内部日志不等于最终HTTP载荷：内部事件可能需要专用序列化或转换，不能从本地缺键就认定内部格式非法、谁把结果id改成了call_id，或CC Switch是唯一根因。客户端历史组装、代理转换、上游兼容性及历史截断/压缩均是待核方向。“同一异常项被后续请求再次携带”只是由重复标识支持的推断。

**处置与验收边界：**停止向同一失败业务会话反复投递，先核对已有副作用并保留现场。不要自行补造工具调用、随机补call_id、删日志或过滤整个heartbeat；这些动作可能丢失唤醒语义、破坏正常工具循环或扩大指令权限。新分支能工作仅说明该恢复样本可用，不证明旧会话修复或所有分支都可避开异常历史。原自动化更新报“实例不存在”另列未解决项，不能据此推断400删除了自动化，也不自动重建。

**反馈建议：**先在官方Issues搜索相同heartbeat和工具结果关联错误，已有同类报告时优先补充环境差异与最小证据。社区评论中的兼容实现、过滤方案和“永久无法恢复”等判断均不当作官方修复结论。最小缺证是故障时实际版本、同一请求在客户端出口及代理转换前后的关联字段、历史传输方式和有效上游信息；无业务副作用的独立复现另获授权，不提交完整请求或业务唤醒正文。验证修复须覆盖原触发、后续普通消息、正常工具调用及无重复业务副作用。

故障反馈以维护者所需的证据为主，GitHub身份可以提供作者入口；不为曝光重复发帖，不在正文夹带无关推广，不承诺流量。公开候选、账号、精确目标与动作仍须操作者最终确认，本条记录不产生发帖授权。

## 三、这次最重要的经验教训

### 1. 短消息成功不等于长任务链路健康

发送“你好”会创建新的 `/responses` 请求，可能继续原任务并写入新的会话状态，因此它不是无副作用的只读探针。若已经发送成功，它最多只能证明：

- 当前账号可以完成一个短请求。
- 当前网络和代理并非完全不可用。

它不能证明：

- 长时间流式连接不会断开。
- 多轮 reasoning 历史可以继续重放。
- 工具调用和工具结果格式兼容。
- 上下文压缩后的密文可以跨 provider 使用。
- 当前额度足以支持一次高 reasoning 的长任务。

### 2. “历史列表统一”和“历史可跨账号继续”不是一回事

CC Switch 的统一历史功能可以让不同来源的任务出现在同一个列表中，但不等于它能把 provider A 生成的加密 reasoning 状态转换成 provider B 可用的状态。

普通聊天文字通常可移植，`encrypted_content`、服务端 response ID、compaction 状态可能和原账号或原上游有关。

### 3. 长任务不能只依赖聊天窗口保存状态

会话上下文属于易失状态。账号、provider、代理、模型、compaction 或失败回滚任一环节变化，都可能让隐藏历史无法继续。

真正可靠的任务状态必须落在项目文件中，至少包括：

- 目标和验收标准。
- 已确认事实与关键决策。
- 已修改文件和当前 Git 状态。
- 已执行的测试及结果。
- 未完成项、风险和下一步。

### 4. 并行恢复可以，双写同一 worktree 不可以

可以让一个任务继续产品开发，同时让另一个任务只读调查会话故障；但两个任务不能同时修改同一个工作目录。

接管时必须明确“唯一写入者”，旧任务暂停后，新任务才能继续修改。

### 5. 连续点击重试可能让现场更复杂

对于普通瞬时网络故障，有限重试是合理的；但对于以下结构化错误，重复原请求通常没有意义：

```text
encrypted_content missing_required_parameter
Encrypted content could not be verified
transform error
validation error
phase missing
```

失败回合还可能不断 rollback，增加定位难度。此时应保留原任务作为审计现场，先从项目已有状态记录或交接快照恢复。只有操作者明确要求继承已完成聊天历史时，才从源任务 Fork。

### 6. “你好”成功不是恢复验收

短消息可能让客户端读取上一轮未完成任务并继续执行，而不是创建一条与旧任务无关的请求。它随后再次报错也不矛盾：一次长任务会产生多轮 `/responses` 请求，某一轮成功不能保证后续轮次成功。诊断时应使用下文定义的固定探针，并记录每次请求发生在首次响应、工具调用前后还是输出过程中。

### 7. 中转比官方直连慢需要定量对照

中转链路通常比官方直连多出本地代理、中转服务器、账号调度、协议转换或重试，因此“体感更慢”在结构上合理。但没有同一时段、同一模型、同一提示词的 A/B 计时，只能记录为观察，不能声称具体慢多少或已定位到某个环节。

### 8. 客户端重连次数不等于人工重试次数

案例环境中，CC Switch 自身存在有限重试，Codex 客户端还会显示“重新连接 5/5”。一次用户操作可能因此产生多次上游请求。服务端日志中连续出现多条 502，不应直接解释成用户手动重复发送了同样数量的请求。排查时应同时记录客户端回合、CC Switch 重试设置和服务端请求 ID，按请求链归组。

## 四、以后长任务的标准状态管理

优先使用项目已经建立的任务状态或交接机制，不要再复制一套事实库。项目没有任何可靠记录、且任务确实较长时，可以使用以下三份文件作为一种实现：

```text
task_plan.md
findings.md
progress.md
```

用途分别是：

| 文件 | 内容 |
| --- | --- |
| `task_plan.md` | 目标、阶段、边界、验收标准、当前状态 |
| `findings.md` | 已确认事实、根因、关键决策、外部资料 |
| `progress.md` | 实际改动、验证结果、未完成项、下一步 |

以下时点应更新项目实际采用的状态机制：

1. 完成一个阶段后。
2. 执行高风险或耗时操作前。
3. 切换账号、provider、模型或中转站前。
4. 上下文即将压缩或任务已经很长时。
5. 准备把任务移交给另一个窗口时。

代码任务还应记录：

```powershell
git status --short
git diff --stat
```

不要把未经提交的代码是否存在、测试是否通过等关键信息只留在聊天文字里。

## 五、切换账号或中转站的安全流程

### 切换前

1. 等待当前回复完成，确认没有工具或终端命令仍在运行。
2. 让当前任务更新项目已有的状态或交接机制；项目没有可靠机制且任务确实较长时，再补充 `task_plan.md`、`findings.md`、`progress.md`。
3. 记录 Git 状态、测试结果、未完成项和下一步。
4. 生成可直接交给新窗口的续接提示词或交接快照；只有操作者明确要求继承已完成聊天历史时才使用 Fork。
5. 暂停原任务的写入职责，确认共享 worktree 只有一个写入者。

不要在模型正在流式输出、工具正在执行或文件正在写入时切换 provider。

### 切换后兼容探针

不要只发送“你好”。应在一个可丢弃的干净测试任务里执行两轮只读测试：

第一轮：

```text
这是 provider 兼容性测试。请执行一次本地只读检查，报告当前工作目录名称，不修改任何文件，然后回复 READY-1。
```

第二轮：

```text
请引用上一轮报告的目录名称，并回复 READY-2；不要再次调用工具。
```

只有两轮都成功，才能证明该账号至少支持当前 Codex 的 Responses、多轮历史和基础工具调用。

### 新任务接管

1. 新任务进入原项目目录。
2. 读取项目实际采用的状态记录或交接快照；只有该项目确实使用三文件方案时，才读取那三个检查点文件。
3. 检查 `git status --short` 和 `git diff`。
4. 复述当前目标、已完成项、未完成项和下一步。
5. 确认旧任务已经停止写入后，再继续修改。

任务 Fork 只在操作者明确要求继承已完成聊天历史时使用；项目状态记录和交接快照才是跨账号接续的主要依据。

## 六、400、401、404、429、502、503、504 快速处置表

| 错误 | 常见含义 | 第一检查项 | 是否重试 | 停止条件 |
| --- | --- | --- | --- | --- |
| `400` + `This conversation has grown too long to continue` | 当前链路明确拒绝继续处理该长会话 | 核对实际失败事件，而非引用文本；恢复项目状态和交接材料 | 不继续向同一失败会话试探 | 保留旧会话，按授权在新任务续接；限制层待确认 |
| `400` + `No tool call found for function call output` | 工具结果与调用关联校验失败；不等于长度超限 | 按第14项核对事件类型、匹配字段和转换前后证据 | 不反复投递旧业务或自行补造配对 | 保留现场与副作用证据；根因层待核，恢复和主动复现分别核授权 |
| `401` | 凭据、登录、授权或账号被上游拒绝 | 当前 provider、账号是否过期、切换是否真正生效 | 不要用同一凭据反复重试 | 修复凭据或切换账号 |
| `404` + `model is not supported by any configured account in this group` | 当前账号组没有支持该模型的账号，或模型映射错误 | 账号组、模型名、模型映射和套餐支持范围 | 不靠切换推理等级重试 | 修复模型映射、账号分组或改用已支持模型 |
| `429` + `rate_limit_error` / `All available accounts are currently rate-limited` | 当前模型线路的可用账号池被限流；不等于用户全局额度耗尽 | 核对错误正文、模型线路和同一时段其他模型的成功/失败对照；确认请求是否产生副作用 | 先停止同一线路的连续重试；等待服务方恢复或按授权切换到已验证的健康模型 | 同一模型线路仍返回429，或服务方尚未确认恢复 |
| `502` | 本地代理无法从上游得到有效响应，或协议转换失败 | `cause`、`upstream_status`、是否出现字段/transform 错误 | 纯上游瞬时错误在无副作用时有限重试；结构化参数错误不重试 | 同时段探针和服务端日志已保存；按错误正文分流 |
| `503` | 服务暂不可用，具体维护、容量或链路原因待确认 | 先核对已写文件及外部副作用，再查provider健康 | 仅无副作用或确认幂等时退避后最多一次 | 仍失败则停止，按授权评估备用线路 |
| `504` | 网关等待上游超时 | 工作区和外部系统是否已经产生副作用 | 仅在确认操作可幂等后重试一次 | 状态不明时先恢复现场，不直接重放 |

补充说明：`429` 的具体含义可能是速率、余额、组织/项目限额或线路账号池限流，不能只凭状态码判断；必须结合错误正文和线路对照。第三方中转也可能重新映射状态码。

### 6.1 429 模型线路级限流的已验证样本

本案例新增一条脱敏记录：某既有任务在单独使用 `gpt-5.2` 经自定义 Responses API 代理请求时，先出现上游 `502`，随后返回 `429 Too Many Requests`。正文为 `rate_limit_error` 及“所有可用账号当前均被限流”的含义，请求未进入正常生成，也没有产生消息项或输入/输出用量。相同时段，同一代理上的 `gpt-5.5`、`gpt-5.6-luna` 和 `gpt-5.6-sol` 请求持续成功；该任务此前在 `gpt-5.6-sol` 上曾正常运行并触发压缩，因此现有证据不支持“用户全局额度耗尽”或“上下文过长”。

最小恢复顺序是：

1. 保存错误正文、模型名、线路和系统状态，先停止对同一 `gpt-5.2` 线路的重复投递。
2. 在授权范围内切换到已通过同一时段对照的健康模型，例如 `gpt-5.6-sol / low`，先发送无副作用的连通性检查。
3. 检查回复和任务状态确实恢复后再继续业务；不要把一次成功当成原线路已永久修复。
4. 服务方确认账号池解除限流前，不要切回故障模型；若必须切回，先做新的只读探针。

这条 `429` 与本目录已有的 Responses API 工具调用配对缺失 `400` 分开处理：前者是模型线路账号池限流，后者是代理转换或会话链配对问题，不能因为都属于客户端请求失败就合并根因。

连接发送失败型502即使外层状态为502，也不能虚构缺失的 `upstream_status`。先核对实际出站路径、本地连接日志及服务端是否收到请求，不因此直接迁移或回滚会话。

若 `502` 的 `cause`、`upstream_status` 和正文只指向上游 `5xx`，且没有 `encrypted_content`、字段转换或分页谱系错误，无副作用时可以有限重试或等待。需要换线路时，先在可丢弃的干净任务中验证备用线路；正式恢复优先在新任务中读取项目状态记录或交接快照，从已确认断点继续。只有核对上一轮副作用和跨 Provider 兼容性后，才考虑在原任务重放。不要因此迁移会话、Fork 任务或删除密文。`502` 暂时消失不能证明线路已永久修复。

若正文包含 `invalid paginated history lineage` 或 `cutoff byte offset is past the source rollout`，应转为本地分页谱系排查。旧迁移回滚只可能修复这类由父会话被改短造成的问题，不能修复纯上游 `502`。

## 七、固定探针与分流方法

### P1～P5 的可复现测试矩阵

P1～P5 是本案例自定义的稳定性测试，不是 Codex 或 CC Switch 官方测试名称。测试目标和最小要求如下：

| 测试 | 验证目标 | 最小要求 |
| --- | --- | --- |
| P1 | 基础文本请求 | 不调用工具，只返回固定文本 |
| P2 | 较长输入和输出 | 处理一段虚构长材料，输出约 700～1000 字结构化分析 |
| P3 | 连续工具调用 | 依次执行 3 次独立、本地、只读命令；每次拿到结果后再继续 |
| P4 | 工具结果后的下一轮 | 不再调用工具，根据 P3 已有结果返回固定核对文本 |
| P5 | 较长上下文下的工具续轮 | 保留 P1～P4 上下文，再依次执行 3 次独立、本地、只读命令 |

每个模型和推理档位使用一个全新的无上下文窗口，并在该窗口中从 P1 连续执行到 P5。不要把每个 P 分别放进新窗口，否则无法验证上下文累积和工具结果续轮。不同 Provider 做严格对照时，应保持模型、推理档位、提示词、窗口起始状态和测试时间尽量一致；任何未控制变量都要记录。

历史记录报告：Provider A 的 Sol 高和极高各在一个新窗口连续完成P1～P5。Provider B 的一组文本与工具探针用了另一模型，不是严格同模型对照；其Astra五窗口测试有固定成功输出，但现有证据不足以唯一确定窗口组织或真实工具结果继承。不能仅凭固定PASS声称工具续轮正确，也不能反向断言它们一定没有继承上下文。Provider A 的Astra历史基础请求曾以模型映射404失败，该失败属于映射修复前样本。

P1～P5只覆盖有限上下文累积，不是接近窗口上限的压力测试。中文正文、固定PASS、模型不报错和真实工具续轮是不同验收项。修复后出现新错误时先保存现场与已有副作用，不为证明稳定而机械重跑完整矩阵；重新验收先做小规模受控测试，再转入真实使用观察。

### 真实 502 发生后的同时段三点对照

真实任务再次出现纯上游 502 时，优先在较短时间内完成：

1. 同一 Provider、干净窗口：固定文本探针。
2. 同一 Provider、干净窗口：一次本地只读工具调用，等待工具结果后的模型续轮。
3. 原任务窗口：仅在下述安全前置满足时发送固定文本探针；状态不明时跳过。

向原任务发送前，先通过已有状态、日志和可观察的工具/进程状态核对：原轮是否仍在执行、是否已有文件或其他副作用、能否保持在安全停止点。仍在执行、断点不明或无法确认安全停止时，跳过原窗口探针，保留日志，不用短消息催醒旧任务。满足前置且已获本次测试授权后，可发送：

```text
本次仅测试固定文本回复，不恢复或继续任何原任务。不要调用工具、修改文件或配置、发送跨任务消息、启动或停止进程，也不要执行历史待办。只回复：TEXT_PROBE_OK。回复后停止。
```

这段提示词只是行为限制，不能保证模型或宿主绝对无副作用；不能把它替代上述状态核验。原窗口探针被跳过时，下方原任务分支保持“未验证”，不据此恢复复杂任务；恢复须另行核对已有副作用、断点和授权。

若有独立 Provider，再用相同模型和提示词重复前两项。备用 Provider 必须尽量来自不同账号组或上游；否则只能证明另一个本地配置标签可用，不能构成独立对照。

### 如何解释结果

原任务连续失败两次后，不再原地重试，执行以下判断：

```text
同一 Provider 的干净探针也失败？
│
├─ 是 → Provider / 账号组 / 网络 / 代理链路范围
│      ├─ 独立 Provider 成功：优先查原 Provider 的账号池和上游
│      └─ 独立 Provider 也失败：再查本机网络、CC Switch 与共同上游
│
└─ 否 → 原任务固定文本探针是否失败？
       ├─ 是 → 原线程历史、compaction 或请求组装嫌疑上升
       └─ 否 → 原任务固定文本当前可用；原复杂请求是否恢复待核
              ├─ 原复杂任务仍失败：检查负载、请求体和工具续轮差异
              └─ 原复杂任务成功：记录为间歇故障，等待服务端日志
```

探针用于缩小范围，不是根因证明。若探针开始时故障已经恢复，所有探针都成功也只能证明恢复后的状态，不能排除 Provider 短时不可用。

### 需要中转站核查的服务端证据

客户端完成对照后，应向中转站提供精确时间和时区、原始 Provider 标签、模型、完整错误正文，以及失败发生在工具调用前后哪个阶段。请中转站回查：

- 请求 ID、实际账号、账号组、调度节点和上游线路。
- 上游原始状态码与错误正文，是否被中转层统一映射为 502。
- 账号可用性、认证刷新、并发、速率、容量、排队和超时。
- `/responses` 的请求体大小、长上下文、工具结果续轮和协议转换。
- 重试次数、连接复用，以及同一时间前后的成功请求。

没有这些服务端证据时，客户端最多能判断故障范围，不能在账号池、节点、调度、认证或容量之间给出最终根因。

## 八、CC Switch 自动故障转移的正确使用方式

### 检查当前状态

启用前应只读确认当前 `enableFailoverToggle` 值，不要根据本文假设本机已经开启或关闭自动故障转移。检查过程不应修改 CC Switch 配置。

### 启用故障转移的前提

CC Switch 官方手册列出的前提包括：

1. 本地代理服务已启动。
2. Codex App Takeover 已启用。
3. 已配置 failover queue。
4. 已开启 auto failover。

备用队列中只应加入已经通过以下测试的 provider：

- 相同目标模型可用。
- Responses API 格式兼容。
- 两轮历史接续成功。
- reasoning item 可以继续。
- 基础工具调用成功。

### 推荐原则

- 初始最大重试次数建议为 1，避免同一工具请求被多次执行。
- 主 provider 放最稳定的账号，备用项按可靠性排序，不按余额多少排序。
- 定期查看 provider 健康、切换频率、熔断次数和失败原因。
- 不要把不同协议、不同模型映射但“碰巧能聊天”的中转混入自动切换队列。

自动故障转移能降低单 provider 暂时不可用造成的中断，但不能保证正在进行的流式回复无缝迁移，也不能修复已经损坏的 `encrypted_content` 历史。

## 九、恢复方法的优先级

遇到任务断联时，按风险从低到高使用以下方法：

先按错误正文分流。已确认本节长度400或其他明确结构化拒绝时，跳过下列原会话重试，不把通用网络恢复顺序套到该样本；新任务探针与恢复仍分别核对授权。

1. 确认是否只是一次瞬时网络故障；无副作用时有限重试一次。
2. 用干净任务执行两轮兼容探针。
3. 创建干净任务，读取项目状态记录、交接快照和 Git 状态继续。
4. 只有操作者明确要求继承源任务的已完成聊天历史时，才使用 Fork。
5. 必要时先只读评估单个任务文件的备份与恢复条件，不直接修改真实会话。

本目录的 `TRB-004` 保存了历史迁移思路，`TRB-005` 记录了后来发现的分页谱系和回滚清单风险。当前真实安装与真实回滚入口都已暂停，不得把批量清理 `encrypted_content` 当作恢复步骤；只有相关入口通过独立安全审查后，才能重新评估真实使用。

## 十、发生 504 或断流时如何避免重复操作

504 只表示没有及时收到响应，不代表上游一定没有执行。

重试前必须检查：

- 文件是否已经创建或修改。
- Git diff 是否已经变化。
- 测试、构建或长命令是否仍在后台运行。
- 数据库写入是否已经发生。
- 是否已经发送消息、创建远端 Issue、提交 PR、发布或部署。

如果存在外部副作用或状态不明，应先恢复现场、确认幂等性，再决定是否重试。不能简单把同一提示再次发送给另一个账号。

恢复时使用下面的最短提示，避免把失败回合前已经完成的动作再做一遍：

```text
请先核对上一轮已实际完成的动作，仅从未完成断点继续，不重复创建、写入、发送或发布。
```

公开错误样例应使用 `<PROVIDER_NAME>`、`<MODEL_NAME>`、`<THREAD_ID>` 和 `<PORT>`，不得粘贴真实运行标识。

## 十一、需要保存的故障证据

以后遇到错误，至少在本地私有记录中保存以下信息：

- 出错时间。
- 使用脱敏任务别名；真实任务标识仅用于运行时定位，不写入仓库文件或日志。
- 当前 provider 和模型名称。
- 完整错误正文，但删除 API Key、OAuth token 等秘密值。
- `cause` 和 `upstream_status`。
- 出错发生在回复前、流式过程中还是工具执行后。
- 干净两轮探针是否成功。
- 原任务连续重试次数。
- `git status --short` 和最近一次测试结果。

对外求助时使用脱敏任务别名和占位符，不发送真实线程 ID；优先提供精确时间与时区、原始 Provider 标签、模型、完整脱敏错误正文和中转站请求 ID。这些信息足以让后续 AI 或服务提供方判断故障层级，不必重新从所有配置开始调查。

## 十二、长任务推荐开场提示词

以后创建重要长任务时，可以在第一条消息中加入：

```text
这是一个可能跨账号、跨会话的长任务。

请先确认并沿用项目已有的状态或交接机制；如果项目没有可靠机制且任务确实较长，再建立最小状态记录：
1. 每完成一个阶段就更新目标、实际改动、验证结果、未完成项和下一步；
2. 在上下文压缩、账号切换或任务移交前立即写入检查点；
3. 同一 worktree 始终只允许一个任务写入；
4. 连续两次连接失败后停止原地重试，保留现场并从状态记录或交接快照恢复；只有我明确要求继承已完成聊天历史时才使用 Fork。
```

## 十三、不能彻底消除的风险

以下风险无法由本地设置完全消除：

- 第三方中转自身宕机、限流或协议实现不完整。
- 上游账号失效、额度耗尽或服务维护。
- 长连接经过系统代理、局域网或公网时被提前断开。
- 不同 provider 对 Responses reasoning、compaction 和工具调用的兼容差异。
- 504 后上游是否已经执行成功的不确定性。

能从根本上改善的是：不再让任务状态只存在于某个账号或某个聊天窗口中。即使发生断联，也能从项目状态记录或交接快照恢复；本地记录无法消除上游服务故障本身。

## 十四、官方资料与本次记录

- OpenAI Conversation state：<https://developers.openai.com/api/docs/guides/conversation-state>
- OpenAI Compaction：<https://developers.openai.com/api/docs/guides/compaction>（2026-09-10回读；只作API机制依据，不代签当前中转兼容性）
- OpenAI Function calling：<https://developers.openai.com/api/docs/guides/function-calling>（2026-09-10回读；只证明公开调用配对契约，不证明内部heartbeat或具体代理实现）
- OpenAI Reasoning：<https://developers.openai.com/api/docs/guides/reasoning>
- CC Switch 官方仓库：<https://github.com/farion1231/cc-switch>
- CC Switch Failover 手册：<https://github.com/farion1231/cc-switch/blob/main/docs/user-manual/en/4-proxy/4.3-failover.md>
- CC Switch Session Manager：<https://github.com/farion1231/cc-switch/blob/main/docs/user-manual/en/3-extensions/3.4-sessions.md>
- CC Switch v3.19.2：<https://github.com/farion1231/cc-switch/releases/tag/v3.19.2>

历史案例发生时使用 CC Switch v3.19.2。2026-09-08 回查官方 Release，最新正式版已更新到 v3.20.2；升级本身仍不能证明跨 provider 的 `encrypted_content` / reasoning 历史可以兼容。使用前应重新阅读当前发布说明，并用隔离任务验证，不把未发布分支或第三方 Fork 当作默认修复。
