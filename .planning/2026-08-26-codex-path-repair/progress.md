# Progress

## 2026-08-26

- 用户否决原型中的自动归档能力，要求安全收缩为“批量修复异常”与“按 ID/标题分析、确认后修复指定任务”。
- 删除脚本、GUI 和重新编译后的 EXE 中的归档入口。
- PowerShell AST 语法检查通过。
- 虚构数据库批量测试通过：只修改 1 条异常路径，正常记录和已归档记录均未改变。
- 虚构数据库单任务测试通过：标题唯一匹配、同名拒绝、按 ID 修复和状态回读均符合预期。
- C# GUI 重新编译成功；依赖自检通过。
- 文档已改为“只修复、不归档”的两功能说明。
- EXE 内嵌脚本与源脚本 SHA-256 一致；禁止调用和本地路径扫描通过。
- Codex 运行中拒绝写库测试通过，拒绝后虚构数据库未发生变化。
- GUI 启动冒烟测试通过，窗口标题和进程状态正常。
- 最终 EXE SHA-256：`EDFB39FF0C41B76575B2908E2110325A0F115C8FF69744DFBC320D08DB6B06D5`。
- 用户要求按“扫描可在线、修复必须离线”重新设计并执行对抗式审查。
- 审查发现 GUI 提示时机过早，以及在线扫描两次查询可能出现瞬时计数不一致；已纳入本轮修复范围。
- 批量只读扫描改为单个 SQLite 事务快照，并区分 `BULK_NO_CHANGES` 与 `BULK_FAULTS_FOUND`。
- GUI 已改为先在线扫描；仅在发现异常后提示退出，确认后重新扫描并修复。
- 完成阶段对抗式审查后，在写入前和成功返回前增加 Codex 进程复查，缩小检查与写入之间的时间窗口。
- 在线异常、在线无异常、运行中拒绝写入、离线重新扫描修复、标题分析回归和 GUI 启动测试全部通过。
- EXE 内嵌脚本哈希与源脚本一致；源码和二进制均未发现归档调用或归档状态写入。
- 更新后 EXE SHA-256：`D5157A4ADCF98D59F82FD0B1F29CC1F23FB03D9B99FC594312941336281EB3A8`。

## 已处理错误

- 预期的旧计划目录不存在；改为按现行规划机制建立本轮独立计划目录。
- 旧版 C# 编译器对带引号的资源参数解析失败；改用仓库内纯英文临时构建目录和无引号相对参数后编译成功。
- GUI 子系统直接命令行调用不会等待；自检改用隐藏的等待进程方式后正常回读结果。
