请进行严格的“蒙版局部编辑”，不要重新生成整张图片。

输入图片：
- 图片 1：唯一底图。除蒙版区域外，必须完整保留它的原始像素。
- 图片 2：仅作为目标对象的外观、位置、大小、形状和颜色参考，不得复制或重绘图片 2 的其他内容。

编辑目标：
在图片 1 的【填写具体位置】增加【填写需要生成的内容】，参考图片 2 中对应对象的状态。

蒙版范围：
仅允许修改【填写蒙版位置、大小和形状】。
蒙版应尽量紧贴目标对象，边缘使用少量羽化进行自然衔接。
蒙版以外不得发生任何像素变化。

必须保持不变：
人物身份、面部结构、姿势、表情、眼睛、头发、皮肤颜色、毛孔、皱纹、细微纹理、光照、背景、尺寸、构图和清晰度。

禁止：
禁止整图重绘、重新采样、缩放、平移、美颜、磨皮、锐化、调色、改变光照、改变背景或增加其他对象。

执行要求：
如果生成工具不能保证蒙版外完全不变，请先生成蒙版内的局部素材，再通过确定性蒙版合成到图片 1；不得把肉眼相似冒充像素一致。

输出验证：
1. 输出尺寸必须与图片 1 完全一致；
2. 蒙版外改变像素必须为 0；
3. 报告蒙版位置、修改像素数量和最终文件哈希；
4. 不覆盖原文件，另存为新文件。